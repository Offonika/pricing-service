# CLAUDE.md

@AGENTS.md

Тонкая обёртка для Claude Code: правила проекта живут в `AGENTS.md` (общем для
всех AI-агентов) и подключаются импортом выше. Содержательные правки вносить в
`AGENTS.md`. Правила workspace — в `/opt/MM/CLAUDE.md` и `/opt/MM/AGENTS.md`.
Project-скиллы подключены симлинком `.claude/skills` -> `.agents/skills`.
