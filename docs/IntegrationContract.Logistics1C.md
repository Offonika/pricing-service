# Integration Contract: Logistics 1C -> External Service

Документ фиксирует рабочую карту технических имен 1С/MSSQL для пилота
`Логистика перемещений в приложении Bitrix24`.

Источник истины для документов остается 1С. Внешний сервис читает данные из 1С
read-only и хранит только внутреннее логистическое состояние и аудит. Bitrix24
является основным UX, Telegram и web fallback используют ту же state machine как
резервные интерфейсы.

## 1. Статус проверки

Проверено на живой MSSQL-базе 1С через локальный `ONEC_DATABASE_URL` и по
выгрузке конфигурации `ВыгрузкаКонфиг10.3`.

Подтверждено:

- документ 1С `ПеремещениеТоваров` физически хранится в `_Document178`;
- табличные части документа: `_Document178_VT3822`, `_Document178_VT3839`;
- справочник складов физически хранится в `_Reference80`;
- история событий документа для сканирования хранится в регистре сведений
  `осиОтветственныеЗаДокументы`, физическая таблица `_InfoRg9448`;
- склад-получатель `Транзит  для Москвы` имеет код `РБ0000027`;
- фактический склад-получатель для логистики определяется через реквизит
  `Подразделение` документа: у подразделения есть реквизит `осиСклад`.

## 2. Readiness Gate Для Пилота

Во внешний логистический контур попадают документы `ПеремещениеТоваров`, где
склад-получатель является транзитным складом.

Документ считается готовым к логистической передаче, если одновременно:

1. документ не помечен на удаление;
2. документ проведен;
3. склад-получатель = `Транзит  для Москвы` / код склада `РБ0000027`;
4. документ распечатан;
5. в истории документа есть событие `Отсканирован`.

Флаг `Проверен` в пилоте не обязателен. После появления отдельной роли
сотрудников проверки его нужно добавить в gate.

Флаг `Отправка подтверждена` отдельно в gate не добавляем: по текущей логике
1С документ с требованием подтверждения не проводится без этого флага.

Если документ фактически был распечатан, но `_Fld8954` не проставился, он не
попадает в автоматическую выгрузку и направляется в `manual_review`.
ОТМЕНЕНО (2026-08-28): пользовательский `manual_ready_override`, который
обходит сбой флага печати. В текущем пилоте даже тестовый администратор может
разобрать причину и увидеть документ, но не переводит его в готовые в обход
readiness gate.

## 3. Заголовок Документа

| Бизнес-поле | Объект 1С | SQL-источник | Надежность | Комментарий |
| --- | --- | --- | --- | --- |
| Документ перемещения | `Документ.ПеремещениеТоваров` | `_Document178` | Высокая | Подтверждено по номерам `РБГУ...`, складам и табличным частям |
| Внутренняя ссылка | `Ссылка` | `_Document178._IDRRef` | Высокая | Использовать как стабильный `external_id` в hex-виде |
| Дата документа | `Дата` | `_Document178._Date_Time` | Высокая | |
| Номер документа | `Номер` | `_Document178._Number` | Высокая | |
| Проведен | `Проведен` | `_Document178._Posted` | Высокая | `0x01` = проведен |
| Пометка удаления | `ПометкаУдаления` | `_Document178._Marked` | Высокая | `0x01` = удален/исключить |
| Организация | `Организация` | `_Document178._Fld3813RRef -> _Reference66._IDRRef` | Высокая | |
| Подразделение | `Подразделение` | `_Document178._Fld3818RRef -> _Reference68._IDRRef` | Высокая | По нему определяется фактический склад-получатель |
| Склад-отправитель | `СкладОтправитель` | `_Document178._Fld3819RRef -> _Reference80._IDRRef` | Высокая | |
| Склад-получатель документа | `СкладПолучатель` | `_Document178._Fld3820RRef -> _Reference80._IDRRef` | Высокая | Для пилота отбор по транзитному складу |
| Фактический склад-получатель | `Подразделение.осиСклад` | `_Document178._Fld3818RRef -> _Reference68._Fld8919RRef -> _Reference80._IDRRef` | Высокая | Использовать как точку выгрузки / получателя во всех интерфейсах |
| Штрихкод / место пакета | поле ОСИ | `_Document178._Fld9256` | Высокая | В живых данных значения вида `-00-SAV-04` |
| Отправка подтверждена | `осиОтправкаПодтверждена` | `_Document178._Fld8805` | Высокая | Подтверждено модулем объекта и живыми значениями |
| Распечатан | `ОсиРаспечатан` | `_Document178._Fld8954` | Высокая | Использовать как обязательный gate |
| Проверен | `ОсиПроверен` | `_Document178._Fld8955` | Высокая | В пилоте не gate |
| Цвет/статус ОСИ | `осиЦвет` | `_Document178._Fld8957` | Средняя | Производное поле, не использовать как основной gate |
| Сборщик / ответственный ОСИ | `осиСборщик` | `_Document178._Fld8806RRef -> _Reference69._IDRRef` | Средняя | Для аудита, не для gate |

Подтверждение по модулю объекта `Документ.ПеремещениеТоваров.МодульОбъекта.txt`:

- при проведении есть проверка `осиОтправкаПодтверждена`;
- цвет документа вычисляется по `ОсиПроверен` и `ОсиРаспечатан`;
- при копировании документа сбрасываются `осиОтправкаПодтверждена`,
  `осиРаспечатан`, `осиПроверен`, сборщик, контролер и IP-поля.

## 4. История Сканирования

Логическая сущность 1С: `РегистрСведений.осиОтветственныеЗаДокументы`.

Физическая SQL-таблица: `_InfoRg9448`.

Запись события подтверждена в общем модуле
`ОбщийМодуль.осиПривилегированный.Модуль.txt`:

```1c
рег=регистрыСведений.осиОтветственныеЗаДокументы.СоздатьМенеджерЗаписи();
Рег.Документ=Документ1;
Рег.Дата=дата;
Рег.ФизЛицо=Пользователь1;
рег.Подразделение=Подразделение;
Рег.Действие=?(Навыдачу,"Выдан","Отсканирован");
Рег.РабочееМесто=ПараметрыСеанса.ТекущийПользователь.осиИмяКомпьютера;
Рег.Записать();
```

| Бизнес-поле | Объект 1С | SQL-источник | Надежность | Комментарий |
| --- | --- | --- | --- | --- |
| Документ | `Документ` | `_InfoRg9448._Fld9449_RRRef -> _Document178._IDRRef` | Высокая | Для `ПеремещениеТоваров` тип ссылки `_Fld9449_RTRef = 0x000000B2` |
| Дата события | `Дата` | `_InfoRg9448._Fld9450` | Высокая | |
| Физлицо | `ФизЛицо` | `_InfoRg9448._Fld9451RRef` | Высокая | В части живых строк не заполнено |
| Действие | `Действие` | `_InfoRg9448._Fld9454` | Высокая | Значения: `Отсканирован`, `Распечатан`, `Выдан`, `Печать без принтера` и др. |
| Подразделение | `Подразделение` | `_InfoRg9448._Fld9455RRef -> _Reference68._IDRRef` | Высокая | |
| Рабочее место | `РабочееМесто` | `_InfoRg9448._Fld9844` | Высокая | В живых данных значения вида `-00-SAV-04` |

Для readiness gate нужен факт существования строки:

```sql
EXISTS (
    SELECT 1
    FROM dbo._InfoRg9448 AS scan WITH (NOLOCK)
    WHERE scan._Fld9449_RRRef = d._IDRRef
      AND scan._Fld9449_RTRef = 0x000000B2
      AND scan._Fld9454 = N'Отсканирован'
)
```

## 5. Справочник Складов

| Бизнес-поле | SQL-источник | Надежность | Комментарий |
| --- | --- | --- | --- |
| Склад | `_Reference80` | Высокая | Справочник складов |
| Внутренняя ссылка | `_Reference80._IDRRef` | Высокая | |
| Код склада | `_Reference80._Code` | Высокая | Для транзита Москвы: `РБ0000027` |
| Наименование | `_Reference80._Description` | Высокая | Для транзита Москвы: `Транзит  для Москвы` |

## 6. Справочник Подразделений

Логическая сущность 1С: `Справочник.Подразделения`.

Физическая SQL-таблица: `_Reference68`.

| Бизнес-поле | Объект 1С | SQL-источник | Надежность | Комментарий |
| --- | --- | --- | --- | --- |
| Подразделение | `Справочник.Подразделения` | `_Reference68` | Высокая | |
| Код подразделения | `Код` | `_Reference68._Code` | Высокая | Например `РБ0000015` |
| Наименование | `Наименование` | `_Reference68._Description` | Высокая | Например `ТК «Савеловский» Мобильный` |
| Склад подразделения | `осиСклад` | `_Reference68._Fld8919RRef -> _Reference80._IDRRef` | Высокая | Главный источник фактической точки доставки |
| Место расчета | `осиМестоРасчета` | `_Reference68._Fld9249` | Высокая | Адрес/место расчета из карточки подразделения |
| Порядок | `Порядок` | `_Reference68._Fld9357` | Высокая | |
| Телефон | `осиТелефон` | `_Reference68._Fld9518` | Высокая | |

Пример из живой базы:

- документ `РБГУ0034447`;
- `СкладПолучатель` документа = `Транзит  для Москвы`;
- `Подразделение` = `ТК «Савеловский» Мобильный`;
- `Подразделение.осиСклад` = `Савеловский Мобильный  пав. Т-103 | Т-105`.

## 7. Черновой SQL Для Выгрузки Готовых Документов

```sql
SELECT
    CONVERT(varchar(34), d._IDRRef, 1) AS transfer_external_id,
    d._Number AS document_number,
    d._Date_Time AS document_date,
    d._Fld9256 AS barcode,
    CONVERT(varchar(34), src._IDRRef, 1) AS source_warehouse_external_id,
    src._Code AS source_warehouse_code,
    src._Description AS source_warehouse_name,
    CONVERT(varchar(34), dst._IDRRef, 1) AS document_target_warehouse_external_id,
    dst._Code AS document_target_warehouse_code,
    dst._Description AS document_target_warehouse_name,
    CONVERT(varchar(34), dep._IDRRef, 1) AS department_external_id,
    dep._Code AS department_code,
    dep._Description AS department_name,
    CONVERT(varchar(34), dep_wh._IDRRef, 1) AS logistics_target_warehouse_external_id,
    dep_wh._Code AS logistics_target_warehouse_code,
    dep_wh._Description AS logistics_target_warehouse_name,
    d._Posted AS posted,
    d._Fld8805 AS send_confirmed,
    d._Fld8954 AS printed,
    d._Fld8955 AS checked,
    d._Fld8957 AS osi_color
FROM dbo._Document178 AS d WITH (NOLOCK)
JOIN dbo._Reference80 AS src WITH (NOLOCK)
    ON src._IDRRef = d._Fld3819RRef
JOIN dbo._Reference80 AS dst WITH (NOLOCK)
    ON dst._IDRRef = d._Fld3820RRef
LEFT JOIN dbo._Reference68 AS dep WITH (NOLOCK)
    ON dep._IDRRef = d._Fld3818RRef
LEFT JOIN dbo._Reference80 AS dep_wh WITH (NOLOCK)
    ON dep_wh._IDRRef = dep._Fld8919RRef
WHERE d._Marked = 0x00
  AND d._Posted = 0x01
  AND dst._Code = N'РБ0000027'
  AND dep_wh._IDRRef IS NOT NULL
  AND d._Fld8954 = 0x01
  AND EXISTS (
      SELECT 1
      FROM dbo._InfoRg9448 AS scan WITH (NOLOCK)
      WHERE scan._Fld9449_RRRef = d._IDRRef
        AND scan._Fld9449_RTRef = 0x000000B2
        AND scan._Fld9454 = N'Отсканирован'
  );
```

## 8. Payload Для Синхронизации

Минимальный payload во внешний сервис:

| Поле сервиса | Источник 1С | Комментарий |
| --- | --- | --- |
| `source_document_type` | константа `transfer` | Нужно для совместимости с РТУ |
| `external_id` | hex `_Document178._IDRRef` | Стабильный ключ документа |
| `document_number` | `_Document178._Number` | |
| `document_date` | `_Document178._Date_Time` | |
| `source_warehouse.external_id` | hex `_Document178._Fld3819RRef` | Дополнительно передавать код и имя склада |
| `document_target_warehouse.external_id` | hex `_Document178._Fld3820RRef` | Для gate это транзитный склад |
| `target_warehouse.external_id` | hex `_Reference68._Fld8919RRef` | Фактический склад-получатель из `Подразделение.осиСклад` |
| `department.external_id` | hex `_Document178._Fld3818RRef` | Хранить для аудита и пересинхронизации |
| `barcode` | `_Document178._Fld9256` или подтвержденный код с печатной формы | Требует уникальности для поиска во всех интерфейсах |
| `onec_status.posted` | `_Document178._Posted` | |
| `onec_status.printed` | `_Document178._Fld8954` | |
| `onec_status.checked` | `_Document178._Fld8955` | |
| `onec_status.send_confirmed` | `_Document178._Fld8805` | Для аудита |
| `onec_status.has_scan_event` | `EXISTS _InfoRg9448 / Отсканирован` | Обязательный gate |
| `payload.raw` | исходные `_Fld...` и refs | Для расследований и пересборки mapping |

Важно: `СкладПолучатель` документа и `target_warehouse` внешнего сервиса - не
одно и то же поле. В документе `СкладПолучатель` может быть транзитным складом,
а фактическая точка доставки для backend и всех интерфейсов берется из связанного склада
подразделения.

В живой выборке есть документы, которые проходят readiness gate, но имеют
пустой `_Document178._Fld9256`. Это не блокирует пилот, если печатная форма
содержит другой штрихкод/QR. Текущий пользовательский процесс сканирует уже
напечатанный код документа, а backend нормализует его и однозначно ищет
логистическую единицу по внутренним полям `lookup_code`/`barcode` и ссылке
документа.

Если выбран не `_Document178._Fld9256`, уникальность нужно проверять уже для
выбранного lookup-поля. Нельзя оставлять уникальный индекс только по `_Fld9256`,
если фактически бот сканирует другое значение.

### 8.1. Расширение payload для `ORDER_TRANSFER_V1`

Для нового потока одна строка SQL view соответствует одной обязательной или
необязательной упаковке актуальной версии плана. Дополнительно передаются:

| Поле | Источник/правило |
| --- | --- |
| `origin_order_external_id` | ссылка `ЗаказПокупателя` |
| `site_order_number` | номер интернет-заказа |
| `flow_mode` | константа `ORDER_TRANSFER_V1` |
| `plan_key`, `plan_version` | стабильный ключ и версия плана |
| `unit_key` | одна пара источник/назначение в рамках заказа |
| `internal_order_external_id` | связанный `ВнутреннийЗаказ` |
| `transfer_external_id` | связанное `ПеремещениеТоваров`, может быть пустым до сборки |
| `expected_unit_count`, `is_required` | агрегация готовности клиентского заказа |
| `is_posted`, `is_printed`, `is_assembled`, `is_deleted` | readiness gate |
| `delivery_code`, `external_carrier` | выбор обычного самовывоза или терминала перевозчика |
| `updated_at` | контроль свежести; scan блокируется после 180 секунд |

Backend читает `dbo.MM_LogisticsOrderTransferV1` только read-only. Разные
клиентские заказы не объединяются в документы, даже если едут одним рейсом.
Повторный pull нормализуется в те же plan/unit, а `ПеремещениеТоваров` остаётся
`source_document_type=transfer`.

Обратный carrier feed — отдельный read-only GET:

`GET /api/logistics/orders/carrier-confirmations?format=xml`

1С проверяет `origin_order_external_id`, `plan_key/version`, терминальный склад,
трек и `source_ref`; затем в одной транзакции заполняет `ДатаПередачиВТК` и
`осиТрекНомер`, ставит статус `06`, создаёт/проводит одну финальную РТУ с
виртуального терминального склада и сохраняет ссылку в плане. Повтор возвращает
ту же РТУ. Конфликт значений или сторонняя РТУ закрывает автоматический путь.

SQL view, cron apply и регламентное задание 1С пока не активированы. Их включение
возможно только после native-8.2 merge/readback и отдельного разрешения.

## 9. QR/Штрихкод Печатной Формы

На текущей печатной форме `ПеремещениеТоваров` уже есть линейный штрихкод, но
его значение не напечатано текстом рядом с кодом. По снимку формы нельзя
надежно определить, что именно он кодирует. В текущем этапе печатную форму не
меняем: сотрудник сканирует уже напечатанный QR/штрихкод, а backend поддерживает
совместимые короткий и полный форматы. Перепечатывать документы не требуется.

ОТМЕНЕНО ДЛЯ ТЕКУЩЕГО ЭТАПА (2026-08-28): обязательное добавление отдельного
корпоративного QR `MMLOG1|transfer|...`. Его печать и рабочий регламент
перенесены на следующий этап. Форматы ниже остаются зарезервированным вариантом
будущей доработки, а не условием текущего пилота.

Рекомендуемый payload QR:

```text
MMLOG1|transfer|<1c_document_ref>
```

Где:

- `MMLOG1` - версия формата;
- `transfer` - тип источника, совместимый с будущим `source_document_type`;
- `<1c_document_ref>` - стабильная ссылка 1С на документ
  `Документ.ПеремещениеТоваров`.

Предпочтительно использовать внутреннюю ссылку документа, а не короткий номер с
печатной формы. На форме номер может отображаться как `34486`, тогда как в SQL
номер документа может содержать полный префикс. Короткий номер без типа и ссылки
нельзя считать надежным lookup.

Если быстро получить ссылку 1С в QR технически сложно, допустимый временный
payload для пилота:

```text
MMLOG1|transfer-number|<full_document_number>|<document_date_yyyyMMdd>
```

Например:

```text
MMLOG1|transfer-number|РБГУ0034486|20260428
```

Такой fallback можно использовать только после проверки уникальности пары
`(_Document178._Number, _Document178._Date_Time)` в окне пилота. Значение
`34486` с печатной формы без префикса и даты использовать нельзя.

Будущие артефакты по 1С после отдельного решения:

- добавить QR в макет
  `Документ.ПеремещениеТоваров.Макет.Накладная`;
- рядом с QR вывести короткую подпись `MMLOG1 transfer`, чтобы отличать его от
  старого штрихкода;
- оставить существующий линейный штрихкод на форме, если он используется текущим
  складским процессом;
- в функции печати `ПечатьПеремещениеТоваров()` передавать в макет параметр
  QR lookup;
- на одном документе сравнить значение, которое печатает 1С в QR, с тем
  значением, которое backend получает из MSSQL sync.

Текущее правило backend:

- `lookup_code` уже является внутренним техническим полем, а `barcode`
  сохраняется для совместимости старых scan endpoints;
- пользователь не выбирает и не вводит отдельный корпоративный lookup-код;
- lookup должен распознавать префикс `MMLOG1|transfer|...` и находить ровно одну
  логистическую единицу, если такой код встречается на существующем документе;
- если найдено несколько документов или ни одного, операция должна уходить в
  ошибку скана, а не создавать движение.

## 10. Контрольные Наблюдения

На живой базе для документов со складом-получателем `РБ0000027`:

- всего непомеченных документов в выборке: `186709`;
- проведенных: `182331`;
- проведенных и распечатанных: `162197`;
- проходящих текущий readiness gate: `101900`.

Эти числа нужны только как sanity check на дату проверки и не являются
бизнес-отчетом.

## 11. Остаточные Проверки

- подтвердить на одном известном документе из UI 1С, что `_Fld8954` точно
  соответствует нижнему флагу `Распечатан`;
- редкий кейс, когда документ фактически печатался, но флаг
  `ОсиРаспечатан` не проставился, должен оставаться в `manual_review` без
  пользовательского обхода готовности;
- подтвердить значение напечатанного кода на живой форме: в выборке
  есть документы, проходящие readiness gate, но с пустым `_Document178._Fld9256`;
  это не блокирует пилот, если печатная форма содержит другой штрихкод/QR, но
  нужно проверить, какое значение реально возвращает сканер;
- проверить однозначность нормализованного значения существующего кода в
  актуальном окне пилота.

## 12. Changelog

- 2026-09-02 — добавлен контракт read-only plan/unit snapshot
  `ORDER_TRANSFER_V1`, freshness/readiness gate и обратный XML carrier feed для
  одной финальной РТУ.
- 2026-08-28 — основным UX назначен Bitrix24; Telegram и web оставлены резервом общей state machine.
- 2026-08-28 — отдельный корпоративный QR/lookup-код и изменение печатной формы перенесены на следующий этап; текущий процесс использует уже напечатанный QR/штрихкод и внутренний `lookup_code` backend.
- 2026-08-28 — пользовательский `manual_ready_override` готовности отменен; сбой признака печати направляется в `manual_review`.
