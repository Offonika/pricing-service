<!-- Archived implementation task. -->
Recommended Agent: pricing-devops

# S4.14a — Довести контур контроля схемы `Розница -> Возврат -> Не розница` до боевого режима

## Цель
Завершить боевой ввод уже реализованного контура контроля возвратной схемы: применить миграцию, включить окружение, расписание и подтвердить рабочий цикл в целевом окружении.

## Что уже реализовано
- Алгоритм детекции, FIFO-матчинг, окно 7 дней и дедуп инцидентов.
- SQL extractor по живой read-only базе 1С.
- Таблица `return_scheme_incident` и миграция `af41c3e7b91a`.
- CLI/job `./.venv/bin/python -m tasks.detect_return_scheme`.
- Генерация `XLSX` и Telegram-уведомления.
- Cron-обвязка и конфигурация в `.env.example`.
- Тесты `tests/test_return_scheme.py`.

## Что нужно сделать
- Применить миграцию `af41c3e7b91a` в целевой БД.
- Заполнить и проверить боевые env-переменные:
  - `ONEC_DATABASE_URL`
  - `RETURN_SCHEME_ENABLED`
  - `RETURN_SCHEME_ALERT_TELEGRAM_TOKEN`
  - `RETURN_SCHEME_ALERT_TELEGRAM_CHAT_ID`
  - `RETURN_SCHEME_OUTPUT_DIR`
- Подключить `cron` или `systemd` в нужном окружении для ежедневного запуска.
- Выполнить боевой dry-run:
  - `./.venv/bin/python -m tasks.detect_return_scheme`
- Проверить результаты dry-run:
  - создаются ли записи в `return_scheme_incident`;
  - сохраняется ли `XLSX`;
  - уходит ли Telegram-уведомление в нужный чат.
- Подтвердить операционные параметры:
  - точный чат/канал;
  - время ежедневного запуска;
  - кто отвечает за разбор отчёта.
- При необходимости прогнать полный `pytest` по репозиторию.

## Артефакты/файлы
- инфраструктурный конфиг окружения;
- миграции Alembic;
- `infra/cron/return_scheme_monitoring.sh`
- `infra/cron/return_scheme_monitoring.cron`
- `infra/cron/return_scheme_alert.py`
- runbook/заметки по эксплуатационным параметрам.

## Definition of Done
- Миграция применена в целевой БД.
- Боевые env-переменные заданы и проверены.
- Расписание реально включено в целевом окружении.
- Dry-run завершён успешно без технических ошибок.
- Подтверждено, что:
  - инциденты записываются в БД;
  - `XLSX` формируется;
  - Telegram-уведомление приходит в целевой чат.
- В плане/заметках зафиксированы получатель, время запуска и ответственный за разбор отчёта.

## Фактический статус
- Выполнено на сервере `A`:
  - применены миграции `af41c3e7b91a` и `c2a7f8b4d5e6`;
  - включены `RETURN_SCHEME_ENABLED=true`, `RETURN_SCHEME_OUTPUT_DIR`, `RETURN_SCHEME_INTERNAL_API_TOKEN`;
  - прямой Telegram-канал на `A` переведён в fallback через `RETURN_SCHEME_DIRECT_TELEGRAM_ENABLED=false`;
  - добавлен и опубликован internal API:
    - `GET /api/internal/alerts/return-scheme/pending`
    - `GET /api/internal/alerts/return-scheme/{batch_id}/report`
    - `POST /api/internal/alerts/return-scheme/{batch_id}/ack`
- Выполнено на сервере `B`:
  - развернут consumer `return_scheme_pull_from_a.py`;
  - включён cron на `09:10` MSK;
  - маршрут доставки настроен через существующий Telegram-бот `mastermobile`;
  - B24-эскалации включены на webhook Bitrix24.
- Расписание:
  - `A`: `09:00` MSK — детекция и формирование batch/report;
  - `B`: `09:10` MSK — pull pending batch, Telegram delivery, B24 escalation, ack.
- Текущий операционный маршрут:
  - Telegram-получатель: канал `Уведомления` (`chat_id -1003442325420`);
  - Bitrix24 responsible: `6759` (Арсен Сагиян, руководитель сети торговых точек);
  - observers: пусто;
  - ответственный за разбор отчёта: Андрей Платонов.
- Подтверждённый боевой прогон:
  - `./.venv/bin/python -m tasks.detect_return_scheme` создал batch `#1`;
  - зафиксировано `58` новых инцидентов;
  - `XLSX` сформирован в `reports/return_scheme/2026-03-19/return-scheme-20260319-230627.xlsx`;
  - consumer на `B` доставил batch и подтвердил `ack`;
  - итоговое состояние batch `#1`: `status=delivered`, `linked=58`, `notified=58`;
  - создано `8` дедуплицированных B24-эскалаций по правилам повторов/критичной суммы.

## Остаток после go-live
- Если потребуется вынести возвратные риски в отдельный канал, переключение маршрута делается только через `RETURN_SCHEME_ALERT_TELEGRAM_TOKEN` и `RETURN_SCHEME_ALERT_TELEGRAM_CHAT_ID` на сервере `B`, без правок кода.
