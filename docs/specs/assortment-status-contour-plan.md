---
spec_id: "assortment-status-contour-plan"
title: "Assortment Status Contour Canonical Plan"
doc_type: spec
domain: "procurement"
status: accepted
owner: "operations"
source_of_truth: true
related_code:
  - app/services/assortment_lifecycle_signal_ingestion.py
  - app/services/assortment_lifecycle_signal_sources.py
  - tasks/build_assortment_signal_ingestion_dry_run.py
  - tasks/build_assortment_signal_source_bundle.py
  - app/models/procurement_order_formation.py
  - app/api/procurement_order_formation.py
  - app/services/procurement_order_formation.py
  - app/services/procurement_order_formation_workspace.py
  - ui/src/components/ProcurementOrderFormationApp.tsx
  - ui/src/components/ProcurementOrderFormationWorkspace.tsx
related_tests:
  - tests/test_assortment_lifecycle_signal_ingestion.py
  - tests/test_assortment_lifecycle_signal_sources.py
  - tests/test_build_assortment_signal_ingestion_dry_run_task.py
  - tests/test_build_assortment_signal_source_bundle_task.py
  - tests/test_procurement_order_formation.py
  - tests/test_procurement_order_formation_workspace.py
  - tests/test_procurement_order_formation_api.py
  - tests/test_procurement_order_formation_dry_run.py
contracts:
  - openapi.yaml
depends_on: []
supersedes:
  - docs/specs/procurement-order-auto-order-unified-contour.md
rollout_required: false
updated_at: "2026-08-17"
---

# Канонический план контура статусов ассортимента

Этот документ фиксирует единственный актуальный план продолжения работ по
статусам ассортимента и приложению `Формирование заказа`. План согласован
`2026-07-16` и заменяет два старых конкурирующих списка: `9` шагов до испытания
и `6` шагов до автономного запуска из исторического единого контура.

Документ задаёт порядок и границы работ. Детальная архитектура реализованного
OAuth-приложения остаётся в
`docs/specs/procurement-order-formation-smart-process.md`, а правила обмена
статусов на стороне 1С — в спецификации проекта `1C_Dev_Workflow`.

# Source of Truth

- Этот документ — единственный актуальный источник порядка следующих работ и
  согласованных межсистемных границ контура статусов ассортимента.
- `docs/specs/assortment-lifecycle-policy.md` — канонический источник самой
  формулы жизненных статусов: лестница, пороги, переходы и гистерезис. Этот план
  задаёт порядок работ и не дублирует формулу.
- `docs/specs/onec-stock-effective-availability.md` — источник дневных фактов
  наличия товара, на которых строятся переходы по спросу.
- `docs/specs/procurement-order-formation-smart-process.md` — технический
  источник поведения OAuth-приложения.
- 1С остаётся источником торгового факта; формула нетинга и правила обмена на
  стороне 1С принадлежат проекту `/opt/MM/1C_Dev_Workflow`. Жизненный статус
  ассортимента в `УТ 10.3` не выгружается: решение `2026-08-05`.
- `procurement-order-auto-order-unified-contour.md` и
  `procurement-decision-contract-roadmap.md` — только историческая справка;
  оба отменены `2026-08-05` и не могут переопределять этот план.
- Судьба семи legacy-правил старого автозаказа подробно разобрана в
  `docs/specs/assortment-status-legacy-rule-inventory.md`: наличие записи,
  отчёта или JSON-конфигурации там не приравнивается к работающей автоматике.

# Change Summary / Spec Delta

- Было: два конкурирующих старых плана, две поверхности и противоречивые правила
  создания черновика.
- Стало: один согласованный план, OAuth-приложение как рабочая поверхность,
  обязательное подтверждение количества и `ДН` как внутренняя очередь.
- Не меняется: 1С остаётся источником торгового факта; production и внешние
  системы этим документом не изменяются.

## Зафиксированные решения

| Тема | Каноническое решение |
| --- | --- |
| Рабочая поверхность | Отдельное OAuth-приложение Bitrix24 `Формирование заказа`; скрытый smart-процесс не используется как рабочая поверхность |
| Термин до передачи в 1С | `Заказ на подтверждении`; это внутренний кейс со строками, расчётом и аргументами, а не документ 1С |
| Создание заказа | Робот автоматически считает и создаёт кейс; любой черновик `ЗаказПоставщику` создаётся только после подтверждения итогового количества сотрудником закупки, который обрабатывает проект |
| Ответственность за очередь | Очередь общая для сотрудников закупки с действующим доступом. Любой такой сотрудник может открыть и обработать проект; система фиксирует фактического пользователя действий. Назначение основного менеджера по папке, постоянного заместителя, резервной роли и таймера переключения не используются |
| График проверки | Потребность рассчитывается и показывается ежедневно. График задаёт только следующий срок и приоритет проверки человеком, но не блокирует создание или видимость внутреннего проекта. До подтверждения автопересчёт защищает только вручную изменённые количество и цену конкретной строки, а остальные строки продолжает обновлять; новая рекомендация по защищённой строке показывается как расхождение. Исчезнувшая потребность не удаляется молча. После подтверждения замораживается весь проект, а последующая потребность создаёт отдельную ревизию |
| Проведение в 1С | Автоматическое проведение запрещено |
| `ДН / Добираем` | Отдельная очередь дополнительной потребности, параллельная лестнице стадий. Она может быть открыта поверх `Заказали` или `Завезли` и не меняет стадию карточки. ОТМЕНЕНО (2026-08-13): уточнение от `2026-08-03`, по которому передача первого заказа в cargo переводила карточку в `Завезли` |
| `Первая партия в пути` | Автоматически вычисляемый логистический признак внутри `Заказали`, а не отдельная стадия. Достоверное cargo-событие включает признак; первый физический складской приход снимает его и подтверждает `Завезли`. Потребность пересчитывается ежедневно и при новом сильном сигнале, карточка проверяется человеком не реже одного раза в `3–5` дней |
| Сигналы скрытого спроса | КМП4, заказ сайта, настоящий резерв, дефицитная корзина и подтверждённый менеджером звонок с точными SKU и количеством являются сильными количественными сигналами. Поиск, просмотр, клик, неподтверждённое упоминание в звонке и отсутствие у конкурента только повышают приоритет. Система открывает или обновляет `Добираем`, но не создаёт заказ в 1С автоматически |
| Граница текущего этапа | Приоритет — закупочный контур: расчёт потребности, формирование внутренних проектов заказов и рабочее место закупщика. Competitor matching используется только как read-only защитный сигнал. Детальный разбор, очистка и исправление конкурентных связей отложены до отдельного решения и не входят в текущий этап; сохранённые результаты аудита остаются справочным контролем, а не очередью на изменение matching |
| `ПН / newborn_need` | Legacy-значение в исторических данных: новые записи запрещены, существующие карточки мигрируются только отдельным проверяемым действием |
| Структурный пол и новая точка заказа | В текущем коде `STRUCTURAL_FLOOR_QTY = 13` остаётся временным предохранителем и сам не означает «заказать до 13». Отдельно решением `2026-08-16` только для карточек **Растим (ПРОДАЖА)** группы «Маржинальный поток» (`0,1–0,25` шт/день и рентабельность выше `31%`) принята целевая точка заказа: `ceil(скорость × 80) + ceil(скорость × 25) + 13`; из неё вычитаются свободный остаток и надёжный товар в пути. **Поддерживаем (Рабочий)** исключён. Фиксированные `13` добавляются один раз. Единственный сигнал — достижение всей точки заказа; отдельной тревоги по свободному остатку `13` нет. Новая формула имеет статус `spec accepted / code pending`; код и production не изменены. BRT, Croston/SBA/TSB и профильный min/max продолжают сравниваться отдельно |
| Брак | Два жёстких блокера: подтверждённый брак выбранного поставщика `>10%` на базе `≥100` проданных единиц; либо аварийный сигнал партии `≥5` дефектов и одновременно `≥40%` продаж. Брак товара без точной связи с поставщиком требует предупреждения и решения закупщика. Для точного поставщика база `<30` только информационная, `30–99` — предупреждение и ручная проверка; автоматического обнуления в этих случаях нет |
| Выбор поставщика | Два этапа: сначала жёсткая допустимость по активности, договору, валюте, актуальной цене, возможности заказа и обязательным документам; затем прозрачная лестница допустимых кандидатов: успеть до дефицита, отсутствие блокера качества, полная известная стоимость, при разнице стоимости `≤3%` — скорость и стабильность, затем MOQ/кратность, условия оплаты и история исполнения. При разнице `>3%` выбирается меньшая полная стоимость среди успевающих кандидатов. Фиксированные веса на первом этапе не применяются. Ничья или недостаток данных не скрывают внутренний проект, но требуют выбора из 2–3 кандидатов и подтверждения поставщика перед 1С |
| Уверенность расчёта | Не единый балл `0–100`, а объяснимые категории: `red` — есть hard blocker; `yellow` — блокера нет, но есть слабый или неизвестный некритичный фактор; `green` — обязательные данные свежие и полные, поставщик определён, предупреждений и блокеров нет. Жёлтая категория требует решения закупщика перед 1С, но не обнуляет количество и не скрывает проект. Numeric score допускается только как shadow |
| Лимиты суммы и количества | На текущем этапе не применяются. Внутренний проект сохраняет полную рассчитанную потребность, а сумма и количество отображаются закупщику без обрезки, обнуления и автоматического перевода в `yellow` только из-за величины. Бюджетный контроль откладывается до утверждения бюджетной модели |
| Будущие исследования | Надёжность свойств 1С, BRT и другие методы редкого спроса, профильный min/max, сезонность, источники плана/прогноза и обязательные профили ручной проверки решаются только после аудита данных или shadow/backtest. Эти темы не блокируют создание и показ текущих внутренних проектов |
| Инвентаризация | Переданный итог — `2729 из 2729`, `100%`; дата запуска, `run_id`, границы выборки и ссылка на итоговый артефакт ещё должны быть восстановлены |
| 12 болей | По переданному контексту все 12 разобраны; точный перечень и обоснования должны быть перенесены из исходного handoff-файла без реконструкции по памяти |

# Implementation Checklist

Единственный актуальный план состоит ровно из следующих шести шагов.

### 1. Закрепить главный документ приложения

Назначить спецификацию отдельного OAuth-приложения `Формирование заказа`
главным техническим источником по интерфейсу, API, правам, состоянию и обмену с
1С.

Критерий завершения: новые документы ссылаются на спецификацию приложения и не
описывают smart-процесс как рабочую поверхность.

### 2. Вывести старый единый контур из актуальных источников

Пометить `procurement-order-auto-order-unified-contour.md` историческим и убрать
у него признак `source_of_truth`. Сохранить файл как объяснение происхождения
формул и решений, но не использовать его старые открытые вопросы и планы как
текущую очередь работ.

Критерий завершения: в manifest старый документ имеет статус `superseded`, а этот
план объявляет его заменённым.

### 3. Закрепить безопасный шлюз перед 1С

Во всех правилах использовать одну последовательность:

```text
автоматический расчёт и проверки
-> кейс Формирование заказа
-> подтверждение итогового количества сотрудником закупки, который обрабатывает проект
-> непроведённый черновик ЗаказПоставщику в 1С
```

Критерий завершения: ни одна актуальная спецификация не разрешает создание
черновика без подтверждения человека; автоматическое проведение запрещено.

### 4. Закрепить ДН как очередь и подготовить миграцию ПН

Новые правила не записывают `newborn_need`. Товар из очереди `Добираем`
сохраняет свою стадию: до физического складского прихода —
`newborn / Заказали`, в том числе с признаком `Первая партия в пути`; после
подтверждённого физического прихода — `new_item / Завезли`. ОТМЕНЕНО
(2026-08-13): прежнее правило, по которому одного cargo-события было достаточно
для перехода в `Завезли`. Перед миграцией нужно read-only посчитать существующие
`newborn_need`, проверить каждую группу исключений и подготовить обратимый план
перевода в `newborn`.

Критерий завершения: новые записи `newborn_need` отсутствуют; существующие
карточки проинвентаризированы; массовая миграция не выполняется без отдельного
разрешения и контрольного отчёта.

### 5. Развести три значения слова «Потребность»

| Термин | Что означает | Что не означает |
| --- | --- | --- |
| Расчёт потребности | Рекомендация `pricing-service`: спрос, остаток, путь, аналоги, поставщик, риски и количество | Не бухгалтерский факт 1С и не разрешение создать документ |
| Экран/представление `Потребность` | Пользовательское отображение расчёта и причин в приложении | Не отдельная формула и не система хранения факта |
| Нетинг `Потребности` в 1С | Учетная формула вычитания обеспечивающих документов и определения непокрытого количества | Не бизнес-интерфейс приложения и не жизненный статус товара |

Критерий завершения: новые документы всегда уточняют, о каком из трёх понятий
идёт речь; формула нетинга остаётся в `1C_Dev_Workflow`.

### 6. Собрать проверяемый статус проекта

В этом каноническом документе должны быть восстановлены:

- точные формулировки всех 12 болей и основания их закрытия;
- дата и идентификатор прогона `2729/2729`;
- границы выборки и число ошибок;
- ссылка на итоговый артефакт;
- готовность приложения и оставшиеся ограничения;
- следующий пилотный гейт без автоматического проведения документов.

Отдельная глубокая инвентаризация семи legacy-блоков завершена в
`docs/specs/assortment-status-legacy-rule-inventory.md`. Она закрывает вопросы
про эффективные дни, каналы спроса, перемещение до закупки, аналоги, брак и
scoring, график заказа, старые вкладки и регламент ответственного. Остальные
неизвестные детали 12 болей и прогона `2729/2729` по-прежнему не
реконструируются догадками.

Критерий завершения: каждый факт имеет ссылку на versioned-документ, код, тест,
run или итоговый артефакт. До восстановления исходника неизвестные детали не
дополняются догадками.

## Текущий статус выполнения

| Шаг | Статус на 2026-08-09 | Что осталось |
| --- | --- | --- |
| 1 | Выполнен; спецификация приложения синхронизирована с политикой `2026-08-09` | Использовать спецификацию приложения во всех новых ссылках |
| 2 | Выполнен; `2026-08-05` отменены также roadmap и brief | Не возвращать старые списки в рабочий план |
| 3 | Выполнен в актуальных спецификациях | Проверять правило при последующих изменениях backend и rollout |
| 4 | Правило закреплено; `newborn_need` новым карточкам не присваивается | Read-only инвентаризация legacy-карточек и отдельный план миграции |
| 5 | Термины разведены в этом документе | Переносить определения в новые адресные документы без копирования формулы 1С |
| 6 | Частично; девять legacy-блоков инвентаризированы | Восстановить исходные 12 болей и техническое доказательство `2729/2729` |

Появилось после согласования плана `2026-07-16` и в него не входило:

| Что | Где зафиксировано | Состояние |
| --- | --- | --- |
| Канонический источник формулы жизненных статусов | `docs/specs/assortment-lifecycle-policy.md` | accepted `2026-08-05`; связка сборщика фактов и fail-safe наличия не завершены |
| Витрина дневного наличия из 1С | `docs/specs/onec-stock-effective-availability.md` | accepted `2026-07-27`; в расчёт заказа входит как теневой показатель |
| Отмена выгрузки жизненного статуса в `УТ 10.3` | политика жизненного цикла, запрет в коде обмена | выполнено `2026-08-05`, ready-пакеты в карантине |
| Отмена консолидации аналогов | `docs/specs/assortment-status-legacy-rule-inventory.md`, раздел 4 | выполнено: решение `2026-07-26`, qty-diff `+2765` шт на `236` карточках, код удалён `2026-07-31`; подтверждено владельцем `2026-08-09` |
| Правила выхода из «Поддерживаем» и медленных/маржинальных карточек | там же, разделы 8 и 9 | спроектированы, в код не переносились |
| Целевая модель редкого спроса и точка заказа «Маржинального потока» | этот документ; правило — `docs/specs/assortment-status-legacy-rule-inventory.md`, раздел 9; каноническая область — `docs/specs/assortment-lifecycle-policy.md` | в коде структурный пол `13` временно остаётся без расширения; для «Маржинального потока» документально принята точка заказа `потребность на 105 дней + 13`, но её реализация ожидает отдельного code/diff/rollout; BRT, Croston/SBA/TSB и min/max сравниваются отдельно |

## План внедрения cold-start workflow от 2026-08-15

Этот раздел задаёт порядок работ для семейства iPhone 17 Pro Max и не создаёт
вторую формулу: бизнес-правила принадлежат
`assortment-lifecycle-policy.md`, а поведение рабочего места —
`procurement-order-formation-smart-process.md`.

| Этап | Результат | Зависимость | Критерий безопасного завершения |
| --- | --- | --- | --- |
| A. Решения и словарь | Утверждённые определения сигналов, режимов, метрик, ролей и stop-условий | Нет | Решения внесены в canonical specs; docs validators проходят |
| B. Point-in-time контракты | Append-only сигнал с `occurred_at`, `available_at`, source, reliability и SKU/family linkage | A | Fixtures доказывают дедупликацию, отсутствие future leakage и раздельный учёт заказов/корзин |
| C. Детерминированное ядро | Семейная потребность, распределение `качество + конструкция`, три режима, netting остатка/pipeline и версионирование правил | B | Pure tests воспроизводимы; одинаковый input/version даёт одинаковый output; early cap `0` |
| D. Backtest v3 | Daily roll-forward, matured cohorts/right-censoring, согласованный P75, консервативный pipeline credit и единый hash-bound bundle | C | Все независимые окна проходят строгий non-degradation; bundle полностью hash-verified |
| E. Журнал и read-only API | Неизменяемые рекомендации и решения `accepted/modified/rejected/deferred` | D | Advisory-only, reason validation, stale conflict, RBAC и append-only audit покрыты тестами |
| F. Рабочее место | Диапазон без prefill, `Диапазон не доказан`, объяснение сигналов и уверенности | E | UI не может создать заказ или изменить статус; accessibility и API contract tests проходят |
| G. ИИ-советник | Read-only объяснение конкретных входов и версии правил | E; не блокирует F/H | Нет tools и write-path; ссылки только на факты рекомендации; отказ при нехватке данных |
| H. Shadow | Ежедневные рекомендации без исполнения и сверка с решениями закупщика | E–F | Не менее 30 рабочих snapshots и 10 значимых решений; SLA и полнота журнала соблюдены |
| I. Ограниченный пилот | Шесть недель advisory-only на одной семье с ручным решением | H | Все stop-условия соблюдены; при недостатке наблюдений пилот продлевается |
| J. Решение о расширении | Отдельный допуск recommendation-led/автоматизации и позднее — обучения | I | Strict non-degradation на каждом независимом окне; отдельное явное бизнес-решение |

Backtest v3 выполняется до разработки API и UI, а не параллельно с ними.
iPhone 17 Pro Max проверяет workflow; окончательный cold-start validation ждёт
следующую новую семью. Первый live-пилот остаётся advisory-only, текущий процесс
закупки авторитетен. Переход к recommendation-led режиму возможен только при
одновременном выполнении на каждом независимом окне: served sales `≥ control`,
missed gross profit `≤ control`, average inventory capital `≤ control`, GMROI
`≥ control`, ending excess `≤ control`, ending shortfall `≤ control`.

Статус на 2026-08-15: этап A завершён; B–D реализованы в read-only backtest v3
настолько, насколько позволяет frozen-история. Гейт D не пройден:
`0/1 728` кандидатов выполнили все ограничения, historical `available_at`
неполон, независимый новый cold-start запуск отсутствует. Поэтому E–J не
начинаются. Следующий безопасный шаг — контрактно сохранять `available_at` для
новых сигналов и зарегистрировать следующую новую семью до её первого решения;
это не разрешает production или внешние записи.

Статус реализации на 2026-08-17: подготовлен и полностью протестирован
неактивированный пакет этапа B для новых сигналов: additive append-only таблица,
контракт `assortment_signal.v1`, обязательные `occurred_at`/`available_at`,
source identity, reliability, SKU либо versioned family linkage, идемпотентная
запись, fail-closed конфликт содержимого и as-of чтение без future leakage.
`site_order` и `site_cart` разделены, Wordstat остаётся direction-only. Alembic-
ревизия изолирована от pending application branch и сведена отдельным merge;
проверены upgrade/downgrade и защита строк от update/delete. Миграция не
применялась, ingestion/cron/API/UI не подключались, production, Bitrix24, 1С,
заказы и статусы не изменялись. Поэтому операционное накопление сигналов ещё не
началось, гейт D остаётся провален, а этапы E–J по-прежнему закрыты.

Решение от 2026-08-17: следующий шаг этапа B — реализовать
`signal-ingestion-v1` только в dry-run. Первая волна включает продажи, остатки,
заказы и поступления поставщиков и cargo; использует единый display-scope с
`excluded_display_name_bitok`, активный семейный реестр и карантин
неразрешённых SKU; выпускает сверочный отчёт. Миграция production, cron,
API/UI и внешние записи этим решением не разрешены.

Реализация от 2026-08-17: подготовлен изолированный dry-run сервис
`app/services/assortment_lifecycle_signal_ingestion.py` и CLI
`tasks/build_assortment_signal_ingestion_dry_run.py`. Входной bundle имеет
схему `assortment_signal_source_bundle.v1`, явный timezone-aware `as_of` и
строки только пяти типов первой волны. Каждая строка содержит source identity,
`occurred_at`, `available_at`, reliability с причиной, SKU, наименование и
неотрицательное количество. До подготовки сигнала применяется единый
display-scope; затем SKU разрешается по `code_1c`, `article`, `fact_sku` или
`info_system_code` только в активной версии семейного реестра. Неизвестная,
неоднозначная или не имеющая семейной связи карточка попадает в карантин.
Точный повтор учитывается отдельно и не удваивает сигнал; разные payload одной
source identity блокируют всю конфликтующую identity. As-of проекция включает
только события, у которых и `occurred_at`, и `available_at` не позже cutoff.
Сверка доказывает два баланса: `source = scope included + scope excluded` и
`scope included = prepared + exact duplicate + conflicted + quarantined`, а
также тот же баланс по всем числовым количествам.

CLI требует `--input-json` и `--output-json`; необязательный
`--family-registry-json` принимает только проверяемый read-only snapshot
`display_family_registry_snapshot.v1`. Source-backed bundle может нести этот
snapshot встроенным, чтобы между извлечением и reconciliation не сменилась
версия реестра. Без fixture и встроенного snapshot активный реестр читается
через `session_scope(read_only=True)`. У команды нет `--apply`, persistence или
внешнего write-path; единственный side effect — запись указанного JSON-артефакта.
Миграция signal store не применена, реальные source adapters, cron, API/UI и
production ingestion не подключены. Поэтому этап реализован как кодовый
dry-run, но не активирован и не начал операционное накопление сигналов.

Решение от 2026-08-17: после проверки кода dry-run разрешён следующий
source-backed шаг — подготовить read-only нормализованные выборки
`customer_sale`, `stock_availability`, `supplier_order`, `supplier_receipt` и
`cargo`, затем выполнить один контролируемый reconciliation dry-run с записью
только отдельного проверочного артефакта. Это разрешение не включает сохранение
сигналов в application DB, применение signal-store миграции, cron, API/UI,
изменение 1С, Bitrix24, заказов, статусов или production activation.

Результат от 2026-08-17: source-backed шаг реализован и выполнен на окне
`2026-08-01 00:00 Europe/Moscow` — `2026-08-17 08:08:59 Europe/Moscow`.
Адаптеры читают только проведённые и не помеченные продажи, заказы поставщика и
поступления, текущие итоги склада и непустое подтверждённое поле cargo. Сырые
ссылки 1С остаются внутри адаптера и заменяются хешированными identity;
`available_at` первого снимка равен фактическому завершению извлечения.

Артефакты находятся в
`reports/assortment_lifecycle/2026-08-17/signal-ingestion-first-source-backed-dry-run/`.
Source bundle содержит `19 626` строк: `16 428` продаж, `2 626` снимков
остатка, `229` строк заказов, `343` строки поступлений и `0` cargo. Все `229`
строк-кандидатов cargo не имели подтверждённой даты `_Fld8852`, поэтому cargo-
сигналы не выпущены. Из `2 678` членов активного реестра `52` не вошли в
когорту источника; отдельный read-only readback подтвердил, что все `52`
существуют в 1С, но помечены на удаление. Это источник статуса
`review_required` и отдельный долг реестра, а не основание возвращать карточки
в расчёт.

Reconciliation имеет статус `ready`: подготовлены все `19 626` строк,
quarantine/conflict/exact duplicate отсутствуют, as-of projection содержит
все строки без future leakage, количественный баланс равен
`109 632 = 109 632`. Persistence, signal release, cron, API/UI, production и
внешние записи не выполнялись. Гейт D и запрет на этапы E–J не изменились.

## Границы и риск-гейты

- Этот план не разрешает изменения production, Bitrix24 или данных 1С.
- Решением владельца от `2026-08-10` ежедневный автоматический расчёт «К
  заказу» и создание сгруппированных по поставщикам внутренних `draft` в БД
  `pricing-service` являются штатной работой приложения. Предварительное
  совпадение количества с человеческим решением для этого не требуется:
  закупщик проверяет и корректирует проект в рабочем месте.
- График статуса, скорости или ценового профиля не является `due-gate` для
  сохранения проекта: он определяет срок и приоритет проверки. Автопересчёт
  может обновить нетронутый проект, но после ручного изменения количества или
  цены обязан сохранить решение закупщика и показать новую рекомендацию как
  отдельное расхождение до её явного принятия.
- Передача в 1С выполняется только отдельным ручным действием пользователя и
  создаёт непроведённый `ЗаказПоставщику`; автоматическая передача и проведение
  запрещены. Smart-process до появления документа в 1С не используется.
- Первый реальный тест передачи выполняется только после трёх успешных рабочих
  дней внутреннего автозаказа: выбирается один небольшой проект без блокеров,
  сотрудник закупки, который открыл проект, проверяет поставщика, договор,
  склад, валюту, номенклатуру, количество и цену, затем вручную отправляет
  проект. В 1С проверяются
  непроведённость документа и отсутствие дубля при повторной команде. Общая
  доступность ручной передачи для всех проектов требует следующего отдельного
  решения; автоматическая передача и проведение остаются запрещены.
- Блокеры не отменяют создание внутренних проектов и не скрывают проблемные
  строки из приложения. Они блокируют только ручную передачу конкретного
  проекта в 1С до исправления, исключения или повторного расчёта строки.
- Потребность в закупке определяется по сети. Дефицит отдельной точки сначала
  проверяется как кандидат на перемещение из доступного внутреннего остатка;
  закупка покрывает только непокрытый остаток сетевой потребности. Возраст
  карточки и границы наблюдаемой истории влияют на уверенность, но не включают
  жёсткую дорисовку спроса.
- **ОТМЕНЕНО (2026-08-10) для внутреннего автозаказа:** требование ещё одного
  отдельного решения после технической готовности ежедневных проектов.
  Внутренний автозаказ включается после исправления и проверки двух P0-разрывов:
  общий `--fail-on-blockers` больше не отменяет сохранение дневной партии, а
  автопересчёт не перезаписывает ручные количество и цену. Дополнительная сверка
  рассчитанного количества с решением человека не является гейтом запуска.
- Запись свойства, миграция `newborn_need`, передача в 1С и создание тестового
  документа 1С по-прежнему требуют отдельных решений. До такого решения флаги
  реальной передачи в 1С остаются выключенными.
- Непроведённый черновик `ЗаказПоставщику` в 1С не создаётся без
  зафиксированного подтверждения итогового количества допущенным сотрудником
  закупки; это
  ограничение не относится к внутреннему автоматическому `draft` в БД
  `pricing-service`.
- После каждого изменения формулы выполняется автоматическая ретропроверка
  `tasks/report_display_auto_order_backtest.py`:
  расчёт на прошлой дате против фактических продаж и наличия за последующий
  горизонт. Разовый ручной просмотр выборки человеком этим заменён (решение
  `2026-08-09`). Ретропроверка контролирует качество формулы, но не блокирует
  создание внутренних проектов; подтверждение итогового количества перед
  ручной передачей черновика в 1С сохраняется.
- Чужие изменения `executive_dashboard` и `retail_director` не относятся к этому
  контуру.

# Acceptance Criteria

- [x] Зафиксирован один актуальный план из шести шагов.
- [x] Старые планы объявлены историческими.
- [x] Спецификация OAuth-приложения назначена главным техническим источником.
- [x] Правила подтверждения черновика и хранения `ДН` сформулированы однозначно.
- [x] Семь legacy-блоков глубоко инвентаризированы с отделением отчётов и
  конфигурации от действующей автоматики.
- [ ] Восстановлен точный перечень 12 болей с доказательствами.
- [ ] Зафиксирован технический источник результата `2729/2729`.
- [ ] Проведена read-only инвентаризация `newborn_need`.

# API / Data Contracts

Согласование плана не меняет API или payload. Действующий контракт приложения
зафиксирован в `openapi.yaml` и технической спецификации приложения. Будущая
миграция `newborn_need` должна получить отдельный проверяемый контракт до любых
записей в 1С.

# Tests

- `python scripts/validate_project_docs.py pricing-service` из `/opt/MM`;
- `python scripts/validate_specs.py` из `/opt/MM`;
- `git diff --check` для изменённых документов.

Кодовые тесты не требуются для этого изменения: поведение приложения, API и 1С
не меняется.

# Rollout

Документ вводится сразу как навигационный и плановый источник. Production,
Bitrix24 и 1С не изменяются. Каждый будущий шаг с внешней записью получает
отдельный rollout-план, прямое разрешение, dry-run, readback и возможность
отката.

## Changelog

- 2026-08-17 — реализованы read-only source adapters пяти core-источников и
  выполнен первый source-backed reconciliation dry-run: `19 626` строк,
  row/quantity balances true, `0` quarantine/conflict/duplicate; выявлены `52`
  помеченных на удаление члена активного реестра и отсутствие подтверждённых
  cargo-дат у `229` строк заказов. Production/persistence не включены.
- 2026-08-17 — разрешён первый source-backed reconciliation dry-run пяти
  core-источников только с read-only доступом и записью отдельного артефакта;
  persistence и любые операционные изменения по-прежнему запрещены.
- 2026-08-17 — реализован, адресно протестирован и зарегистрирован изолированный
  `signal-ingestion-v1` dry-run первой волны: scope до сигналов, active family
  registry, aliases SKU, карантин, exact dedup/conflict block, as-of без future
  leakage и row/quantity reconciliation; persistence, cron, API/UI и production
  не включены.
- 2026-08-17 — утверждён dry-run `signal-ingestion-v1` первой волны с
  core-сигналами, единым scope, семейной привязкой, карантином и reconciliation
  report; production activation не разрешена.
- 2026-08-17 — подготовлен и протестирован неактивированный пакет point-in-time
  signal store этапа B: append-only схема и сервис, source-level дедупликация,
  `occurred_at`/`available_at`, as-of чтение, раздельные site order/cart и
  direction-only Wordstat. Миграция не применена, ingestion и production не
  подключены; backtest gate D и запрет на E–J не изменились.
- 2026-08-17 — текущий приоритет возвращён в закупочный контур: competitor
  matching оставлен только как read-only защитный сигнал; детальный разбор и
  исправление конкурентных связей отложены до отдельного решения, а результаты
  выполненного аудита сохранены как справочный контроль.
- 2026-08-16 — план синхронизирован с принятым правилом для группы
  «Маржинальный поток» только в статусе **Растим (ПРОДАЖА)**:
  **Поддерживаем (Рабочий)** исключён; точка заказа равна потребности на `105`
  дней плюс фиксированный страховой запас `13` штук; из неё вычитаются
  свободный остаток и надёжный товар в пути. Единственный сигнал — достижение
  точки заказа; отдельной тревоги по свободному остатку `13` нет. Текущий
  `STRUCTURAL_FLOOR_QTY = 13` не выдан за реализацию нового правила. Статус
  `spec accepted / code pending`, production не изменён.
- 2026-08-15 — этап A завершён, read-only B–D проверены backtest v3; гейт D
  провален (`0/1 728`, неполный historical `available_at`, нет независимого
  нового cold-start запуска), поэтому журнал/API, UI, shadow и пилот не начаты.
- 2026-08-15 — добавлен утверждённый зависимый план cold-start workflow
  `A–J`: backtest v3 предшествует API/UI, пилот длится шесть недель и требует не
  менее 30 рабочих snapshots и 10 значимых решений, а расширение допускается
  только при строгом неухудшении каждого независимого окна.
- 2026-08-13 — `Первая партия в пути` закреплена как логистический признак
  стадии `Заказали`; прежний переход `cargo → Завезли` отменён. Для очереди
  `Добираем` закреплены сильные и мягкие сигналы скрытого спроса, ежедневный
  расчёт, проверка человеком раз в `3–5` дней и запрет автоматического заказа в
  1С.
- 2026-07-16 — согласован и зафиксирован новый канонический план из шести шагов.
- 2026-07-17 — добавлена ссылка на завершённую инвентаризацию семи legacy-правил.
- 2026-08-09 — план синхронизирован с решениями конца июля и августа: добавлены
  канонический источник формулы жизненных статусов и витрина дней наличия,
  зафиксированы отмена выгрузки статусов в `УТ 10.3` и отмена обоих старых
  документов, обновлена таблица выполнения. Порядок шести шагов не менялся.
- 2026-08-09 — показ выборки человеку заменён автоматической ретропроверкой
  формулы; первый прогон (as-of `2026-06-10`, горизонт `60` дней): `86%`
  попаданий, перезаказ `~319` тыс руб, упущенные продажи `~407` тыс руб по
  строкам без заказа.
- 2026-08-09 — разрешён ежедневный испытательный расчёт «К заказу» с сохранением
  только внутренних `draft` в БД `pricing-service`; запись в Bitrix24 и 1С,
  XML и проведение документов остались запрещены до отдельных решений.
- 2026-08-10 — создание внутренних проектов по поставщикам переведено из
  испытательного исключения в штатную работу приложения без предварительного
  гейта совпадения с человеком; корректировка остаётся ручной, передача
  непроведённого черновика в 1С — отдельной кнопкой. Smart-process до появления
  документа в 1С не используется.
- 2026-08-10 — проекты со строками-блокерами предписано сохранять и показывать
  в приложении; блокер запрещает передачу конкретного проекта в 1С, но не
  создание внутреннего `draft`.
- 2026-08-10 — разделены закупка и распределение: локальный дефицит сначала
  проверяется на перемещение, а закупается только остаточная потребность сети;
  возраст карточки и `date_min/date_max` используются как признаки надёжности,
  не как основание жёстко дорисовывать продажи.
- 2026-08-10 — структурный пол `13` оставлен временным предохранителем без
  расширения; целевой метод для редкого спроса выбирается после backtest и
  shadow-сравнения BRT, Croston/SBA/TSB и профильного min/max.
- 2026-08-10 — выбран мягкий график: потребность видна и обновляется ежедневно,
  расписание управляет только сроком и приоритетом проверки; ручные изменения
  количества и цены защищены от автоматической перезаписи.
- 2026-08-10 — утверждён порядок включения внутреннего автозаказа: исправить и
  проверить общий `fail-on-blockers` и защиту ручных правок, затем включить
  ежедневные внутренние проекты без дополнительного гейта сверки количества;
  передача в 1С остаётся отдельным rollout-решением.
- 2026-08-10 — для ежедневного автопересчёта выбран построчный merge: ручные
  количество и цена строки защищены, нетронутые строки обновляются, исчезнувшая
  потребность помечается; подтверждение замораживает проект целиком.
- 2026-08-10 — утверждён протокол первого теста 1С: три успешных рабочих дня
  внутреннего автозаказа, один небольшой проверенный проект, проверка
  непроведённого документа и защиты от дубля; общий rollout передачи остаётся
  отдельным решением.
- 2026-08-10 — подтверждена двухуровневая защита по браку: точный поставщик
  `>10%` при базе `≥100` или аварийная партия `≥5` и `≥40%` блокируют строку;
  неизвестная атрибуция требует ручного решения, база `<30` остаётся
  информационной, а `30–99` требует ручной проверки; автообнуления нет.
- ПРОМЕЖУТОЧНО (2026-08-10) — подтверждён двухступенчатый выбор поставщика: eligibility
  выполняется до scoring; ничья или недостаток данных не скрывают проект, но
  блокируют передачу в 1С до подтверждения закупщиком; вопрос метода scoring
  закрыт следующей записью.
- ПРОМЕЖУТОЧНО (2026-08-10) — для supplier scoring выбрана прозрачная лестница
  приоритетов; фиксированные веса не применяются, а оставшийся тогда порог
  близкой стоимости закрыт следующей записью.
- 2026-08-10 — порог близкой полной стоимости поставщиков установлен в `3%`:
  внутри коридора приоритет у скорости и стабильности, выше порога — у меньшей
  полной стоимости среди кандидатов, успевающих до дефицита.
- 2026-08-10 — единый порог confidence `80/85/90` отменён; приняты объяснимые
  категории `green/yellow/red`, numeric score допускается только как shadow.
- ОТМЕНЕНО (2026-08-10) — решение о мягких лимитах суммы и количества с
  профильным `yellow`: проверка переписки восстановила более раннее прямое
  решение владельца работать без лимитов до появления бюджетной модели.
- 2026-08-10 — на текущем этапе лимитов суммы и количества нет: приложение
  показывает полную потребность и стоимость без обрезки, обнуления и `yellow`
  только из-за величины; бюджетный контроль отложен до утверждения бюджетной
  модели.
- ОТМЕНЕНО (2026-08-10) — ответственность за очередь закреплена за основным исполнителем
  товарной папки, резерв — за закупочной ролью без постоянного именного
  заместителя. Причина: для текущей общей очереди отдельное назначение
  избыточно.
- ОТМЕНЕНО (2026-08-10) — гибридное подключение резерва: сразу при отмеченном
  отсутствии основного ответственного, иначе после одного рабочего дня без
  действия по проекту. Причина: резервная маршрутизация и таймер не нужны общей
  очереди.
- 2026-08-10 — очередь проектов сделана общей для допущенных сотрудников
  закупки: кто открыл и взял проект в работу, тот его обрабатывает; система
  фиксирует фактического пользователя, отдельного назначения и замещения нет.
- 2026-08-10 — финальный аудит документации отделил закрытые решения от
  реализации и будущих исследований; устаревшая персональная проверка пилота
  заменена общей очередью и автоматической ретропроверкой формулы.
