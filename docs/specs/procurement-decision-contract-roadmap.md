---
spec_id: "procurement-decision-contract-roadmap"
title: "Procurement Decision Contract And Roadmap"
doc_type: spec
domain: "procurement"
status: draft
owner: "operations"
source_of_truth: true
related_code:
  - scripts/ensure_procurement_bitrix_process.py
  - scripts/import_onec_supplier_order_to_procurement.py
  - app/services/procurement_supplier_crm.py
  - app/services/assortment_lifecycle.py
  - app/services/assortment_lifecycle_facts.py
  - app/services/exporters/ut103_exchange.py
  - app/services/exporters/ut103_nomenclature_properties.py
  - tasks/build_assortment_lifecycle_facts.py
  - tasks/build_assortment_lifecycle_updates.py
  - tasks/export_ut103_nomenclature_properties.py
related_tests:
  - tests/test_procurement_bitrix_process.py
  - tests/test_procurement_supplier_crm.py
  - tests/test_assortment_lifecycle.py
  - tests/test_assortment_lifecycle_facts.py
  - tests/test_build_assortment_lifecycle_facts_task.py
  - tests/test_build_assortment_lifecycle_updates_task.py
  - tests/test_ut103_exchange.py
  - tests/test_ut103_nomenclature_properties_exporter.py
  - tests/test_export_ut103_nomenclature_properties_task.py
contracts: []
depends_on:
  - docs/Onepage.ProcurementManagementContour.md
  - docs/specs/ved-akb-import-pilot.md
supersedes: []
rollout_required: true
updated_at: "2026-06-26"
---

# Назначение

Сделать техническое задание на следующий шаг контура управления закупками:
программа должна готовить безопасный проект заказа поставщику, объяснять каждую
строку и создавать непроведенный черновик `ЗаказПоставщику` в `1С` только после
подтверждения человеком.

Главная мысль: автоматизируем не "покупку вслепую", а управляемое решение. У
каждой строки заказа должен быть понятный ответ:

- что купить;
- сколько купить;
- почему купить именно столько;
- у какого поставщика;
- какая цена подтверждена;
- какие есть риски;
- почему строку можно или нельзя отдавать в черновик 1С.

Связанный onepage: `docs/Onepage.ProcurementManagementContour.md`.

# Простые термины

Сложное слово не убираем, а пишем рядом понятную расшифровку:

- контракт решения (`decision contract`) - набор полей, по которым программа
  объясняет строку закупки;
- подтверждение человеком (`approval`) - человек разрешил создать черновик в 1С;
- доказательство (`evidence`) - файл, скрин, запись, ссылка, счет или другое
  подтверждение цены, срока, документа или обещания поставщика;
- ручная проверка (`review`) - программа не уверена и зовет человека;
- документальная остановка (`document gate`) - дальше нельзя двигаться, пока не
  приложены обязательные документы;
- минимальное количество заказа (`MOQ`) - меньше этого поставщик не отгружает;
- срок до поступления (`lead time`) - сколько дней пройдет от решения купить до
  товара на складе;
- период между заказами (`order cadence`) - как часто мы вообще можем
  пересматривать и отправлять заказ по этой папке;
- буфер задержки поставщика (`supplier delay buffer`) - запас дней на обычные
  опоздания поставщика или логистики;
- доступные дни продаж (`available sales days`) - дни, когда товар реально был
  в наличии в торговых точках и мог продаваться;
- дни без товара (`out of stock days`) - дни, когда товара не было и продажи
  нельзя считать как "нулевой спрос";
- оценка уверенности (`confidence score`) - насколько программе можно доверять
  по этой строке;
- расчетная единица закупки (`procurement calculation unit`) - уровень, на
  котором программа реально считает потребность: SKU, группа аналогов,
  `предмет + tag`, группа свойств/характеристик, поставщик, потребность магазина
  или ручной профиль;
- метод спроса (`demand method`) - способ посчитать будущий спрос для расчетной
  единицы: средняя по доступным дням, сезонная средняя, BRT, потребность
  магазина или ручная проверка;
- BRT - метод для редкого, но реального спроса: программа смотрит историю
  интервалами поставки и выбирает количество под целевой уровень наличия. BRT
  не применяется к разовой продаже одному клиенту и не отменяет стоп-сигналы;
- профиль закупочного поведения (`procurement behavior profile`) - лучший
  рабочий признак после папки: группа правил, которая говорит, как этот товар
  обычно заказывается, готовится, едет, стоит и рискует;
- паспорт ассортимента (`assortment passport`) - очищенный набор признаков
  товара: папка, `Предмет`, категория, бренд, модель, совместимость, качество,
  характеристики, ценовой сегмент и текущий ассортиментный статус;
- ассортиментная матрица (`assortment matrix`) - утвержденный список товаров,
  которые должны быть в работе для категории, магазина, формата или периода;
- статус ассортимента (`assortment status`) - управленческий статус товара:
  матричный, рабочий, новинка, тест, под заказ, неликвид, снят, не закупать;
- ценовой сегмент (`price segment`) - уровень товара внутри узкой категории по
  цене/себестоимости: топ, средний, эконом или более детальная шкала;
- БКГ-по-русски (`internal BCG`) - анализ товара по двум внутренним осям:
  доля в наших продажах и динамика наших продаж;
- сезонный коэффициент (`seasonality factor`) - насколько в этот месяц/неделю
  обычно растет или падает спрос по группе;
- активная номенклатура периода - сколько товаров группы реально было в продаже
  или могло продаваться в анализируемом месяце/неделе;
- качество ассортимента - простыми словами, насколько группа состоит из
  продающихся товаров, а не из большого числа мертвых позиций;
- план продаж / прогноз продаж - сколько бизнес ожидает продать; план может
  быть управленческой целью, прогноз - расчетом по истории и сезонности;
- потребность магазина - заявка конкретной точки, например `Потребности
  Савелова`, которую нужно учитывать отдельно от факта продаж;
- свободный остаток - товар, который можно использовать для продаж или
  перемещения;
- фактический остаток - физически числится на складе, но часть может быть
  зарезервирована, заблокирована, в браке или не готова к продаже;
- товарная позиция (`SKU`) - конкретный артикул/номенклатура.

## Что значит `АКБ без документов`

`АКБ без документов` - это не просто "нет бумажки". Это значит, что у команды нет
подтверждения, что аккумулятор можно безопасно и законно купить, перевезти,
ввезти и принять на склад.

Для АКБ заказ не должен идти дальше, пока не проверены минимум:

- `MSDS` - паспорт безопасности материала;
- `UN38.3` - подтверждение, что литиевый аккумулятор прошел транспортные тесты;
- сертификат или декларация соответствия, если требуется для товара;
- `ТН ВЭД` - код товара для таможни;
- `invoice` или proforma invoice - счет/инвойс от поставщика;
- `packing list` - упаковочный лист;
- страна происхождения;
- условия перевозки и подтверждение, что cargo/логист берет такой груз;
- связь документов с конкретным поставщиком, товаром и партией.

Если этого нет, программа может показать потребность, но не должна автоматически
создавать заказ поставщику.

# Scope / Out of Scope

Входит:

- контракт решения по строке закупки;
- настройки по папкам номенклатуры;
- расчетные единицы закупки: SKU, группа аналогов, `предмет + tag`, свойства,
  характеристики, поставщик, потребность магазина или ручной профиль;
- автоматический выбор метода спроса: средняя по доступным дням, сезонная
  средняя, BRT для редкого реального спроса, потребность магазина, ручная
  проверка;
- расчет потребности с учетом продаж, остатков, товара в пути и TopControl;
- трехлетний анализ продаж по номенклатурным группам: сезонность, изменение
  ассортимента, доля продающихся позиций, сильные и слабые места закупки;
- учет плана продаж, прогноза продаж и потребностей магазинов из РММЗ/TopControl;
- разделение активных торговых складов и складов, которые считаются только по
  остатку;
- стоп-сигналы для ручной проверки;
- безопасный выбор поставщика;
- хранение подтвержденных цен и доказательств;
- Bitrix-карточка `Закупка/Заказ`;
- файловый обмен для создания непроведенного черновика `ЗаказПоставщику` в 1С;
- дорожная карта запуска от read-only расчета до пилота с черновиком 1С.

Не входит:

- автопроведение документов 1С;
- прямой SQL-write в 1С;
- автоматическая оплата поставщика;
- автоматический заказ АКБ/ВЭД без документов;
- полная WeChat-интеграция как первый шаг;
- замена TopControl или существующих обработок без отдельного решения.

# Change Summary / Spec Delta

Было:

- закупочные правила живут частично в 1С, TopControl, обработках, WeChat, Disk и
  опыте сотрудников;
- программа может считать рекомендации, но нет единого контракта объяснения
  строки;
- поставщик, документы, цена и доказательства не связаны в один безопасный
  процесс.

Станет:

- у каждой строки есть единый контракт решения;
- система работает не одной общей формулой, а по папкам, расчетным единицам
  закупки и профилям;
- метод спроса выбирается по данным и профилю товара, а не задается одной
  настройкой для всего каталога;
- спорные строки блокируются до ручной проверки;
- подтвержденные цены и доказательства сохраняются;
- после подтверждения Омаром Бочаровым создается непроведенный черновик
  `ЗаказПоставщику` через файловый обмен;
- Bitrix показывает весь путь: расчет, подтверждение, заказ, оплата, логистика,
  приемка.

Не меняется:

- 1С остается учетной системой;
- TopControl остается источником важных признаков;
- Bitrix не становится единственной аналитической базой;
- человек отвечает за опасные решения и исключения.

# Source of Truth

| Данные | Главный источник |
| --- | --- |
| Номенклатура, контрагент, договор, валюта, склад, заказ, поступление | `1С` |
| Заказы поставщику, состав партий, дата создания, дата сдачи в cargo, подчиненные поступления | `1С` |
| Статусы дефицита, сверхнорматива, неликвида, ABC/XYZ | `TopControl` |
| Продажи по товару, группе, магазину и периоду | `1С`, с проверкой против TopControl |
| `Предмет`, `tag`, свойства, характеристики, совместимость и аналоги | `1С`/РММЗ/TopControl после проверки заполненности |
| Паспорт ассортимента, ценовой сегмент, аналитический статус | `pricing-service`, расчетно из 1С/TopControl/продаж |
| Рекомендуемое изменение статуса ассортимента в УТ 10.3 | `pricing-service`, затем файловый обмен с `УТ 10.3` |
| Примененный текущий статус ассортимента до перехода в КА | `УТ 10.3`, через свойства номенклатуры и штатную обработку обмена |
| Утвержденная ассортиментная матрица после перехода в КА | `1С КА`, типовой контур управления ассортиментом |
| Предложения по вводу/выводу товара, новинкам, неликвиду, BCG/ABC/XYZ | `pricing-service`, затем ручное утверждение |
| План/прогноз продаж | TopControl/РММЗ/управленческий план, после подтверждения источника |
| Потребности магазинов, например `Потребности Савелова` | `Заказы покупателя` в 1С/РММЗ/TopControl после корректировки помощником |
| Рабочая карточка закупки, статусы, ответственный, файлы | `Bitrix` |
| Подтвержденная цена, история условий, evidence | `pricing-service` плюс ссылка в Bitrix |
| Переговоры и исходные подтверждения | `WeChat/Disk`, после переноса в evidence |
| Правила папок номенклатуры | Настройки контура закупок, утверждает отдел закупки |

# Data Flow

```text
1С / TopControl / помощник закупок / продажи / остатки / заказы поставщику / открытые заказы / план / потребности магазинов / WeChat evidence / Disk-видео
-> нормализация данных
-> повторный разбор правил из пилотных записей 37976 и 37992
-> разбор заказов поставщику за 6 месяцев по составу и датам
-> трехлетний анализ продаж и сезонности по номенклатурным группам
-> выбор папки номенклатуры
-> построение паспорта ассортимента и аналитического статуса товара
-> проверка будущей совместимости с 1С КА: категория / статус / матрица / ввод-вывод / квота
-> выбор расчетной единицы: SKU / аналог / предмет+tag / свойства / поставщик / потребность магазина
-> выбор профиля закупочного поведения внутри папки
-> выбор метода спроса: средняя / сезонная средняя / BRT / потребность / ручная проверка
-> расчет потребности
-> проверка поставщика, цены и документов
-> procurement_decision_contract.v1
-> очередь решений в Bitrix
-> подтверждение человеком
-> файл обмена для 1С: прогноз / заказ / изменение статуса ассортимента
-> непроведенный черновик ЗаказПоставщику
-> контроль оплаты, логистики и приемки
```

# Контур управления ассортиментом и подготовка к 1С КА

Закупочный контур нельзя строить без отдельного слоя управления ассортиментом.
Иначе программа будет хорошо считать количество для товаров, которые уже нужно
вывести, заменить, оставить только под заказ или тестировать малой партией.

## Архитектурное решение

На этапе `УТ 10.3` `pricing-service` строит read-only аналитический слой:
паспорт ассортимента, статус товара, рекомендации ввода/вывода и объяснение.
Он не становится учетной системой и не пишет напрямую в SQL 1С.

После перехода на `1С КА` типовой модуль "Управление ассортиментом" должен
стать нормативным местом для утвержденной ассортиментной политики:

- товарные категории;
- ассортиментные статусы;
- ввод и вывод товаров из ассортимента;
- ассортиментная матрица по магазинам, форматам, категориям и периодам;
- квоты по количеству SKU в категории;
- коллекции/сезоны, если они применимы к группе.

`pricing-service` при этом остается расчетным слоем: он предлагает статус,
объясняет причину, показывает риск и готовит карточку решения для человека.
Утвержденные решения затем можно переносить в `1С КА` как норматив.

## Автоматизация статусов через файловый обмен с УТ 10.3

Расчет статусов можно автоматизировать до перехода в `1С КА`. Текущая `УТ 10.3`
остается местом фиксации примененного управленческого статуса, но не местом
сложной аналитики.

Решение для v1:

- `pricing-service` рассчитывает `recommended_status`, причину, уверенность и
  необходимость ручного утверждения;
- для `УТ 10.3` используем уже существующий файловый контур
  `UT103_EXCHANGE_ROOT`, как в обмене прогнозом продаж;
- фактический тестовый корень обмена: `E:\MMExchange\UT103`;
- прямые `UPDATE/INSERT/DELETE` в SQL-таблицы 1С запрещены;
- запись в 1С делает только внешняя обработка или регламентное задание через
  штатные объекты 1С;
- первый режим всегда `dry_run`: 1С проверяет товар, текущий статус, наличие
  свойства и возвращает результат без записи;
- режим `apply` разрешен только после утверждения пакета человеком;
- жесткие статусы `Не закупать`, вывод из ассортимента и обратное включение
  требуют ручного утверждения даже после запуска автоматизации.

Минимальные свойства в `УТ 10.3`:

| Свойство | Назначение |
| --- | --- |
| `Статус ассортимента` | Текущий управленческий статус товара |
| `Причина статуса ассортимента` | Короткое объяснение, почему статус изменен |
| `Дата изменения статуса ассортимента` | Когда статус был записан |
| `Источник статуса ассортимента` | `pricing-service`, ручное решение, импорт |
| `Утвердил статус ассортимента` | Пользователь/роль, подтвердившая применение |

Если в `УТ 10.3` удобнее не дорабатывать конфигурацию реквизитами, v1 может
использовать механизм `СвойстваОбъектов`/`ЗначенияСвойствОбъектов` для
номенклатуры. Это сохраняет совместимость с будущим переносом в `1С КА`: в КА
эти значения мапятся на типовой контур управления ассортиментом.

## Методические подходы

| Подход | Как применяем | Ограничение |
| --- | --- | --- |
| Ассортиментная матрица | Определяем, какие SKU должны быть в работе по категории/магазину/формату | Не заменяет расчет запасов и поставщика |
| БКГ-по-русски | Сравниваем долю в наших продажах и динамику наших продаж | Это внутренний анализ, не оценка рыночной доли |
| ABC/XYZ | Смотрим несколько показателей: штуки, выручка, валовая прибыль, маржа, стабильность, дефицит | Не применять слепо к новинкам, подзаказу и спецтоварам |
| Контроль новинок | Новинка получает цель: оставить или вывести; дорогие новинки обязательны к ручному контролю | Нельзя масштабировать по первой продаже |
| Ценовой сегмент | Внутри узкой категории видим топ/средний/эконом и каннибализацию | Требует хорошей классификации товара |
| BRT | Используем для редкого, но реального спроса | Не применять к разовой продаже одному клиенту и ко всему каталогу |

## Роли систем

| Решение | Где считать | Где утверждать/хранить |
| --- | --- | --- |
| Паспорт ассортимента | `pricing-service` из 1С/TopControl/продаж | До КА - витрина сервиса; после КА - часть признаков в `1С КА` |
| Аналитический статус | `pricing-service` | Человек утверждает; после перехода фиксируется в `1С КА` |
| Ассортиментная матрица | `pricing-service` предлагает | `1С КА` хранит утвержденный норматив |
| Ввод/вывод товара | `pricing-service` готовит рекомендацию | `1С КА` оформляет изменение ассортимента |
| BRT/ABC/XYZ/BCG/новинки | `pricing-service` | В `1С КА` попадает итоговый статус, не вся расчетная кухня |
| Автозаказ поставщику | `pricing-service` готовит черновик | `1С` хранит документ после подтверждения |

## Методические источники для проверки

Эти источники используются как ориентиры для внутренней методики, но каждое
правило должно быть проверено на наших данных и пилоте:

- BRT-метод: `https://infostena.ru/brt-metod-pri-zakaze-postavshhiku/`;
- БКГ-матрица по-русски: `https://infostena.ru/bkg-matrica-po-russki/`;
- контроль новинок:
  `https://infostena.ru/kontrol-tovarov-novinok-metody-i-ogranicheniya/`;
- ценовой сегмент:
  `https://infostena.ru/cenovoj-segment-osnova-professionalnogo-upravleniya-assortimentom/`;
- модуль "Управление ассортиментом" в `1С КА` как будущий нормативный контур.

# Принятые правила из повторного разбора видео

Видео 37976 и 37992 важны не как "демо экрана", а как источник логики решения.
В них видно, что текущий помощник закупок уже показывает часть нужных сигналов,
но человек поверх них применяет дополнительные правила. Эти правила нужно
перенести в расчет, чтобы программа сама готовила правильный проект заказа.

## Видео 37976: очередь `К ЗАКАЗУ` и фильтры до заказа

- Сортировка по `К ЗАКАЗУ` нужна как очередь разбора: сначала смотреть строки,
  где программа предлагает больше всего. Но сама цифра `К ЗАКАЗУ` еще не
  означает, что строку можно отправлять поставщику.
- Товары со словом `дубль` в названии не должны попадать в заказ. Для таких
  строк нужен блокер `do_not_order`, пока закупка явно не снимет запрет.
- Кроме слова `дубль`, нужен ручной флаг `не закупать`: его ставит отдел
  закупки, с причиной, автором и датой. Программа обязана исключать такие строки
  до расчета количества.
- Товары `снятый`, товары с заменой, товары с входящей собственной маркой или
  товары, от которых отказались по качеству/бренду, не должны тихо попадать в
  заказ только потому, что есть расчетная потребность.
- Для дисплеев недостаточно папки `дисплеи`: внутри нужны свойства и группы
  качества, например InCell, GX, ORIG100, FSENERGY, Soft OLED, Hard OLED, ZY,
  MINK/MMK, рамка/без рамки, площадка под IC, проверка/верификация.
- Анализ спроса должен отличать здоровую розничную продажу от единичной сделки:
  4 штуки, проданные по разным магазинам/клиентам, сильнее, чем 4 штуки одному
  клиенту или в одном заказе.
- Центральный склад не является обычной торговой точкой. Он может закрывать
  потребность по остатку, но не должен раздувать или занижать среднюю продажу.
- Новинки не масштабируем по первой продаже. Если товар пришел первый раз или
  `ДатаМин = ДатаМакс`, он идет в отдельный контроль, базово каждые 5 дней.
- `Дата рождения`, `ДатаМин`, `ДатаМакс` и количество поступлений нужны для
  статуса товара: новый, юный, обычный, старый, неликвид.

## Видео 37992: флагманы, деньги и цепочка поставки

- Дисплеи обычно имеют общий срок до появления в продаже около 30-35 дней, но
  этот срок должен считаться из фактических дат, а не быть жесткой константой.
- Логистика может быть около 12-15 дней при нормальной ситуации, но срок должен
  быть регулируемым по факту, маршруту и внешним условиям.
- Дорогие товары нельзя покупать крупной партией только потому, что они важные.
  Для них лучше частый, например недельный, пересмотр и меньшие партии, чтобы не
  замораживать деньги и не попасть на падение цены.
- Дешевые флагманы можно закупать реже и больше, если спрос стабильный и риск
  цены ниже. Для них можно проверять покрытие до 80-90 дней, но только после
  расчета денег, скорости продаж и риска остатков.
- Дисплеи нужно делить на 3-4 ценовые группы по себестоимости; для пилота
  принимаем 4 квартиля, чтобы отделить дорогие, средние и дешевые правила.
- В расчете должен быть pipeline, простыми словами "что уже в цепочке": товар в
  компании, товар будет через 10 дней, товар будет через 20 дней, товар уже сдан
  в cargo, товар еще в обработке у поставщика.
- Неликвид: нет продаж 180 дней или продано 3 штуки и меньше за 180 дней при
  старом возрасте товара. Для него действие - проверить цену/остаток, а не
  покупать.
- Если поставщик после запроса заказа дает новую цену и она резко изменилась,
  например 200 -> 150, строка должна получить сигнал скачка цены и ручную
  проверку.

# API / Data Contracts

## `procurement_folder_config.v1`

Настройка папки номенклатуры.

| Поле | Значение |
| --- | --- |
| `folder_id` | ID/код папки |
| `folder_name` | Название: АКБ, дисплеи и т.д. |
| `default_calculation_unit_level` | Базовый уровень расчета в папке: SKU, аналог, `предмет + tag`, группа свойств, поставщик, потребность магазина |
| `calculation_unit_source_priority` | Порядок выбора признаков: свойства 1С, характеристики, `предмет`, `tag`, аналоги, история заказов, ручная настройка |
| `allowed_calculation_unit_levels` | Какие уровни расчета разрешены в этой папке |
| `default_demand_method` | Базовый метод спроса: средняя, сезонная средняя, BRT, потребность магазина, ручная проверка |
| `demand_method_selection_policy` | Правила автовыбора метода спроса по стабильности, ширине спроса, новизне, неликвиду и доступным дням |
| `order_cadence_days` | Как часто формировать заказ |
| `sales_window_days` | За сколько дней смотреть продажи |
| `lead_time_days` | Срок до поступления |
| `supplier_delay_buffer_days` | Запас дней на обычные задержки поставщика/логистики |
| `receiving_buffer_days` | Запас дней на приемку, разбор, постановку на склад |
| `safety_stock_days` | Страховой запас |
| `min_stock_days` | Минимальный уровень наличия, ниже которого нельзя опускаться |
| `max_stock_days` | Верхний уровень наличия, выше которого не закупаем без ручной причины |
| `min_display_qty` | Минимальное количество для нормальной выкладки/наличия |
| `active_sales_warehouse_ids` | Склады, где считаем продажи |
| `sales_point_warehouse_policy` | Какие склады считаются точками продаж: все склады, откуда товар продается систематически, кроме центрального склада и складов брака/несистемных продаж |
| `stock_only_warehouse_ids` | Центральный, транзитные и склады перемещений |
| `reserved_stock_source` | Откуда брать резервы под клиентские заказы |
| `blocked_stock_source` | Откуда брать брак/карантин/товар, который нельзя продавать |
| `allowed_supplier_ids` | Разрешенные поставщики |
| `supplier_lead_time_source` | Откуда брать срок поставки: карточка, история, ручная настройка |
| `abc_xyz_policy` | Как ABC/XYZ влияет на страховой запас и ручную проверку |
| `new_item_policy` | Правило для новинок |
| `nonliquid_policy` | Правило для старых/неликвидных товаров |
| `price_jump_pct` | Порог скачка цены, базово 10% |
| `price_jump_order_window` | Сколько заказов сравнивать, базово 2 |
| `auto_order_min_confidence` | Минимальная уверенность для автосоздания черновика |
| `auto_order_max_risk_level` | Максимальный риск, который можно пускать в автоматику |
| `requires_document_gate` | Нужна ли документальная остановка |
| `approver_user_id` | Кто подтверждает перед 1С |
| `manual_review_rules` | Правила ручной проверки |
| `seasonality_history_months` | Сколько месяцев истории брать для сезонности, базово 36 |
| `seasonality_grain` | Какой шаг анализа: месяц, неделя, неделя+месяц |
| `sales_plan_source` | Где брать план продаж по группе/магазину |
| `forecast_source` | Где брать прогноз продаж: TopControl, расчет сервиса, ручной план |
| `store_need_sources` | Где брать потребности магазинов: РММЗ, TopControl, поля `Потребности ...` |
| `manager_need_signal_policy` | Как хранить персональные сигналы менеджеров: отдельный ТЗ/документ поверх текущего ежедневного документа по подразделению |
| `assortment_quality_policy` | Как изменение количества номенклатуры влияет на заказ |
| `assortment_lifecycle_policy` | Правила переходов `Плод -> Новорожденный -> ПН -> Новинка -> СП -> ПРОДАЖА -> Рабочий` |
| `folder_responsible_user_id` | Ответственный за папку: подтверждает переходы, ручную дорогую классификацию и страховой запас |
| `senior_procurement_user_id` | Старший по закупке: решает, ставить или снимать статус `Эксклюзив` |
| `exclusive_status_policy` | Причины, ручной минимальный остаток, 30-дневный пересмотр и правила публикации для статуса `Эксклюзив` |
| `expensive_item_policy` | Правила `Дорогой быстрый` / `Дорогой медленный`: верхние 25% цены/себестоимости внутри группы, скорость до/от 7 дней, ручная корректировка |
| `procurement_risk_policy` | Как риски трехлетнего анализа меняют страховой запас и автозаказ |
| `supplier_order_analysis_months` | Сколько месяцев заказов поставщику разбирать для профилей, для дисплеев базово 6 |
| `supplier_order_created_at_source` | Где брать дату появления/создания заказа поставщику |
| `cargo_handoff_date_source` | Где брать дату сдачи товара в cargo в заказе поставщику |
| `warehouse_receipt_date_source` | Где брать дату поступления на склад из подчиненных документов |
| `order_cadence_policy` | Как считать график заказа: оборачиваемость рубля + логистика + доля группы в продажах + бюджет |
| `default_purchase_analysis_days` | Базовый период формирования заказа/анализа продаж, в видео используется 60-61 день |
| `new_item_review_cadence_days` | Как часто пересматривать новинки, базово каждые 5 дней |
| `nonliquid_window_days` | Окно неликвида, базово 180 дней |
| `nonliquid_max_sales_qty` | Порог неликвида, базово 3 штуки и меньше за 180 дней |
| `pipeline_stage_policy` | Как раскладывать запас/заказы на 3-4 этапа: в компании, едет, сдано в cargo, у поставщика |

## `assortment_passport.v1`

Паспорт ассортимента - нормализованная карточка товара для аналитики и будущего
перехода в `1С КА`. Это не замена справочника номенклатуры, а очищенный слой
признаков, по которому можно выбирать статус, расчетную единицу и метод спроса.

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `product_ref` | Ссылка/код номенклатуры 1С |
| `article` | Артикул |
| `name` | Наименование |
| `folder_id` | Текущая папка номенклатуры |
| `folder_path` | Полный путь папок внутри `ОБЩИЙ КАТАЛОГ` |
| `kind_1c` | `Вид номенклатуры` |
| `subject_1c` | `Предмет` из надежного объектного свойства 1С |
| `category_1c` | Категория из 1С/TopControl |
| `brand_compatibility` | `Совместим с брендом` |
| `model_compatibility` | `Совместим с моделью` |
| `quality_raw` | Исходное качество: `ORIG100`, `Medium`, `High`, `Аналог` и т.д. |
| `quality_normalized` | Нормализованное качество для расчетов |
| `characteristic_values` | Значимые характеристики по типу товара |
| `price_segment` | Ценовой сегмент внутри узкой категории |
| `assortment_status_current` | Текущий утвержденный статус, если есть |
| `assortment_status_recommended` | Рекомендованный статус по аналитике |
| `assortment_status_reason` | Почему предложен статус |
| `is_matrix_candidate` | Кандидат в матрицу |
| `is_exit_candidate` | Кандидат на вывод |
| `is_new_item` | Новинка по дате первого прихода/продажи |
| `is_nonliquid_candidate` | Кандидат в неликвид |
| `is_do_not_order` | Жесткий запрет закупки |
| `data_quality_score` | Насколько надежны признаки паспорта |
| `missing_required_attributes` | Какие признаки не заполнены |
| `future_ka_mapping_status` | Готово ли поле к переносу в `1С КА`: ready/needs_mapping/manual |

Правила:

- `Предмет` брать из объектных свойств 1С, а не из первого найденного плоского
  свойства;
- папка остается верхней рамкой, но не заменяет `Предмет`, категорию и
  характеристики;
- ценовой сегмент считается внутри узкой товарной категории, а не по всему
  каталогу;
- аналитический статус не должен сразу перезаписывать учетный статус без
  подтверждения человека.

## `assortment_policy_recommendation.v1`

Рекомендация по изменению ассортимента. Она отвечает на вопрос: оставить товар,
ввести в матрицу, тестировать, вывести, перевести под заказ или запретить
закупку.

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `recommendation_id` | ID рекомендации |
| `product_ref` | Номенклатура |
| `calculation_unit_id` | Связанная расчетная единица закупки, если есть |
| `current_status` | Текущий статус ассортимента |
| `recommended_status` | Рекомендованный статус |
| `recommended_action` | `introduce`, `keep`, `test`, `move_to_on_demand`, `exit`, `do_not_order`, `manual_review` |
| `method_codes` | Какие методы участвовали: BCG, ABC/XYZ, new_item_control, price_segment, BRT, seasonality |
| `internal_sales_share` | Доля в наших продажах |
| `internal_sales_trend` | Динамика наших продаж |
| `abc_group` | ABC по выбранному набору показателей |
| `xyz_group` | XYZ по стабильности спроса |
| `price_segment_position` | Позиция в ценовом сегменте |
| `new_item_age_days` | Возраст новинки |
| `similar_product_refs` | Аналоги/замены для сравнения |
| `cannibalization_risk` | Риск, что товары пожирают продажи друг друга |
| `assortment_gap_risk` | Риск, что в сегменте не хватает товара |
| `assortment_bloat_risk` | Риск раздутого ассортимента |
| `stock_money_risk` | Риск заморозки денег в остатках |
| `decision_confidence` | Уверенность рекомендации |
| `human_approval_required` | Нужно ли утверждение человека |
| `approver_user_id` | Кто должен утвердить |
| `ka_target_object` | Будущий объект КА: статус, матрица, ввод/вывод, квота |
| `reason_text` | Человеческое объяснение |

Правила:

- новинки не отправляются в обычный ABC/XYZ без отдельной метки;
- товары под заказ и разовые клиентские потребности не смешиваются с обычной
  матрицей;
- дорогие новинки требуют ручного контроля даже при первых продажах;
- BRT может участвовать в рекомендации по запасу, но не решает вопрос
  "оставить или вывести" в одиночку;
- `Эксклюзив` - ручной ассортиментный статус. Его ставит или снимает старший по
  закупке; рыночный сигнал "есть у нас, нет у конкурентов" может быть
  доказательством, но не ставит статус сам;
- рекомендация должна быть обратима: человек может принять, отклонить или
  отправить на повторную проверку после периода наблюдения.

## Статусы ассортимента v2

Статус отвечает на вопрос "на каком этапе жизни товар". Профиль закупочного
поведения отвечает на вопрос "как его закупать". Поэтому `Дорогой быстрый` и
`Дорогой медленный` остаются профилями закупки, а не статусами.

| Код | Русское название | Точка входа | Работа робота |
| --- | --- | --- | --- |
| `fruit` | `Плод` | Номенклатура создана, первого заказа поставщику еще нет | Собирать сигналы, автозаказ не создавать |
| `newborn` | `Новорожденный` | Появился первый `Заказ поставщику` | Контролировать первое движение товара |
| `newborn_need` | `ПН / Потребность новорожденного` | Есть сигнал спроса: клиент, менеджер, сайт, офлайн, звонок | Сформировать потребность и передать на подтверждение менеджеру/закупщику |
| `new_item` | `Новинка` | Первый заказ в жизни товара сдан в cargo | Частый контроль спроса, расчет примерно на 15 дней запаса |
| `sales_start` | `СП / Старт продаж` | Второй заказ в жизни товара сдан в cargo | Проверять спрос ежедневно; с третьего заказа пересматривать каждые 5 дней, держать 25-30 дней запаса по каждой точке продаж отдельно |
| `sale` | `ПРОДАЖА` | Товар участвовал в документе `Поступление товаров` после этапа `СП` | Раз в 7 дней проверять спрос и формировать заказ примерно на 45 дней запаса |
| `working` | `Рабочий` | За 180 дней с даты `Новинка` товар участвовал в 5 документах `Поступление товаров`; подтверждает ответственный за папку | Автозаказы по графику каждые 15 дней, запас 60 дней с учетом заказов, сданных в cargo |
| `exclusive` | `Эксклюзив` | Ручное решение старшего по закупке | Держать отдельные правила наличия, цены и контроля; показывать на сайте как эксклюзивный продукт; будущие правила записывать в историю статуса |
| `matrix` | `Матричный` | Ручное/категорийное решение | Приоритетный ассортимент, можно считать автозаказ при высокой уверенности |
| `on_demand` | `Под заказ` | Товар нужен только под подтвержденную потребность | Не смешивать со средней продажей |
| `replace_candidate` | `Кандидат на замену` | Есть риск дубля, аналога или замены | Ручная проверка перед закупкой |
| `nonliquid` | `Кандидат на неликвид` | Слабые продажи/старение остатка | Не закупать автоматически |
| `do_not_order` | `Не закупать` | Явный запрет закупки | Исключить до формулы, причина обязательна |

Правила учета переходов:

- первая сдача заказа поставщику в cargo переводит товар в `Новинка`;
- вторая сдача заказа поставщику в cargo переводит товар в `СП / Старт продаж`;
- каждое участие товара в документе `Поступление товаров` считается отдельным
  поступлением для правила 5 поступлений за 180 дней;
- `ПРОДАЖА -> Рабочий` система рекомендует автоматически, но фиксирует только
  после подтверждения ответственного за папку; отдельного "слабого спроса" как
  блокера здесь нет, потому что 5 поступлений за 180 дней уже являются сильным
  математическим сигналом спроса;
- `Эксклюзив` ставит/снимает только Омар как старший отдела закупки. Причина,
  автор, дата, срок пересмотра и публичный признак для сайта обязательны.
  Минимальный остаток не имеет общего значения по умолчанию: его вручную
  задают Омар и ответственный менеджер по папке; пересмотр - каждые 30 дней
  теми же ответственными;
- менеджерские сигналы по `ПН` хранятся отдельно от обычной средней продажи:
  текущий ежедневный документ по подразделению остается, а поверх него нужен
  отдельный персональный ТЗ/документ, где все менеджеры видят новинки и могут
  поставить свой сигнал;
- если менеджер завышает количество, строка должна попадать в будущий контроль
  безопасности как подозрительная потребность.

## `nomenclature_property_updates.v1`

Пакет для передачи утвержденных или проверочных изменений свойств номенклатуры
из `pricing-service` в `УТ 10.3` через файловый обмен. Первый сценарий -
`Статус ассортимента`, но транспорт сразу сделан универсальным для whitelist
свойств.

Папки обмена:

```text
UT103_EXCHANGE_ROOT/
  to_1c/
    new/
      nomenclature_properties_<message_id>.ready.xml
  from_1c/
    new/
      nomenclature_properties_<message_id>.result.xml
```

Текущий тестовый Windows-контур использует `E:\MMExchange\UT103`. В
`pricing-service` путь задается через `UT103_EXCHANGE_ROOT`; на Linux это должен
быть путь к смонтированной папке, ведущей в тот же Windows-каталог.

Пока Windows-папка не смонтирована на Linux, используем локальный outbox
`pricing-service`:

```text
/opt/MM/pricing-service/.local/ut103_exchange/UT103
```

Windows-планировщик забирает оттуда готовые
`nomenclature_properties_*.ready.xml`, кладет их в
`E:\MMExchange\UT103\to_1c\new`, запускает импорт 1С и возвращает result-файлы
в Linux outbox. Скрипты доставки хранятся в
`1C_Dev_Workflow/scripts/windows/`.

Минимальная структура XML:

```xml
<?xml version="1.0" encoding="windows-1251"?>
<ExchangeMessage>
  <Header>
    <MessageId>nomenclature-properties-20260623-001</MessageId>
    <Schema>nomenclature_property_updates.v1</Schema>
    <CreatedAt>2026-06-23T10:00:00+03:00</CreatedAt>
    <Source>pricing-service</Source>
    <Target>1c_ut_10_3</Target>
    <Mode>dry_run</Mode>
  </Header>
  <Items>
    <Item>
      <IdempotencyKey>nom-prop:РБ000074721:Статус ассортимента:2026-06-23:r1</IdempotencyKey>
      <NomenclatureCode>РБ000074721</NomenclatureCode>
      <PropertyName>Статус ассортимента</PropertyName>
      <ValueType>property_value</ValueType>
      <NewValueName>Новинка</NewValueName>
      <NewValueTag>new_item</NewValueTag>
      <ExpectedCurrentValueName></ExpectedCurrentValueName>
      <Reason>Первый приход, товар моложе 90 дней</Reason>
    </Item>
  </Items>
</ExchangeMessage>
```

Правила применения:

- `Mode=dry_run` проверяет пакет без записи;
- `Mode=apply` требует `ApprovedBy` и не применяется, если текущее значение в
  1С отличается от `ExpectedCurrentValueName` / `ExpectedCurrentValueTag`;
- повторный пакет с тем же `IdempotencyKey` должен возвращать `already_actual`
  или `duplicate`, а не менять свойство повторно;
- результат по каждой строке возвращается как `validated`, `applied`,
  `already_actual`, `needs_review` или `failed`;
- whitelist v1: `Статус ассортимента`,
  `Причина статуса ассортимента`, `Дата изменения статуса ассортимента`,
  `Источник статуса ассортимента`, `Утвердил статус ассортимента`,
  `Профиль закупочного поведения`, `Ручной минимальный остаток`,
  `Дата пересмотра правила наличия`;
- код статуса не дублируется отдельным свойством: он хранится в поле `Тэг`
  значения свойства `Статус ассортимента`;
- если в 1С не найдено свойство или значение `property_value`, обработка должна
  вернуть явную ошибку настройки.

## `procurement_calculation_unit.v1`

Расчетная единица закупки - это уровень, на котором программа реально считает
потребность. Папка номенклатуры остается верхним уровнем управления, но не
обязана быть уровнем формулы.

Простыми словами: раньше можно было сказать "считаем дисплеи". Теперь нужно
сказать точнее: "считаем этот SKU", "считаем группу аналогов", "считаем
`предмет + tag`", "считаем дисплеи с таким качеством и рамкой", "считаем
потребность Савелова" или "считаем только ручным профилем".

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `calculation_unit_id` | ID расчетной единицы |
| `folder_id` | Папка номенклатуры |
| `unit_level` | Уровень: `sku`, `analog_group`, `subject_tag`, `property_group`, `supplier_property_group`, `store_need`, `manual_profile` |
| `unit_key` | Стабильный ключ расчетной единицы |
| `sku_refs` | Какие SKU входят в расчетную единицу |
| `analog_group_id` | Группа аналогов/замен, если используется |
| `item_subject` | `Предмет` из карточки/РММЗ/TopControl |
| `item_tags` | Теги, которые влияют на закупку |
| `property_group_key` | Группа свойств, например качество/рамка/верификация дисплея |
| `characteristic_values` | Важные характеристики: модель, совместимость, бренд, качество, рамка, площадка под IC |
| `supplier_split_key` | Признак, по которому заказ нужно дробить между поставщиками |
| `store_need_key` | Потребность магазина или клиента, если расчет идет от заявки |
| `unit_source` | Откуда взят уровень: правило папки, свойство, `tag`, история, ручная настройка |
| `unit_source_confidence` | Насколько надежно заполнены признаки расчетной единицы |
| `manual_owner_user_id` | Кто отвечает за ручной профиль, если он нужен |
| `is_order_split_unit` | Нужно ли по этой единице дробить заказ поставщику |
| `reason_text` | Почему выбран именно этот уровень расчета |

Правила выбора расчетной единицы:

- если SKU уникален и имеет свою историю, можно считать на уровне SKU;
- если товары взаимозаменяемы, считать группу аналогов, а не каждый SKU отдельно;
- если в 1С/РММЗ есть надежные `предмет` и `tag`, использовать их как рабочую
  группу для ассортимента;
- если свойства дисплея определяют поставщика или риск, использовать группу
  свойств/характеристик;
- если поставщик зависит от свойства, расчет можно делать вместе, но заказ
  поставщику нужно дробить по `supplier_split_key`;
- если строка пришла из `Потребности <магазин>`, она остается отдельным
  сигналом, а не смешивается с обычной средней продажей;
- если признаки заполнены плохо, расчетная единица получает низкую уверенность
  и уходит на ручную проверку.

## `procurement_demand_method_policy.v1`

Метод спроса выбирается после расчетной единицы. Один метод на весь каталог
запрещен: стабильный ходовой товар, редкий спрос, новинка, неликвид и разовая
B2B-потребность должны считаться по-разному.

| Метод | Когда использовать | Что важно |
| --- | --- | --- |
| `available_days_average` | Стабильный широкий спрос | Считать только дни, когда товар был доступен в активных точках |
| `seasonal_average` | Есть повторяемая сезонность | Брать 3 года, активные SKU и поправку на дефицит |
| `brt_quantile` | Редкий, но реальный спрос | Не применять к разовой продаже одному клиенту |
| `store_need` | Есть заявка точки или клиента | Учитывать отдельно от факта продаж |
| `manual_review` | Новинка, неликвид, конфликт данных, спорный поставщик | Показывать расчет, но не создавать автозаказ |

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `demand_method_code` | Код метода |
| `method_selection_reason` | Почему выбран этот метод |
| `method_source` | Автовыбор, правило папки, ручная настройка |
| `method_confidence` | Уверенность в выбранном методе |
| `brt_history_days` | Сколько дней истории брать для BRT |
| `brt_supply_horizon_days` | На какой горизонт BRT моделирует потребность |
| `brt_service_level_target` | Целевой уровень наличия для BRT |
| `brt_candidate_qty` | Количество, предложенное BRT до ограничений поставщика |
| `brt_distribution_snapshot` | Краткое описание распределения интервалов/потребности |
| `brt_blocker_reason` | Почему BRT нельзя применить, если метод отклонен |

BRT применяем только если:

- спрос редкий, но не разовый;
- продажи распределены хотя бы по нескольким магазинам, клиентам или периодам;
- товар не новый и не неликвидный;
- доступные дни продаж отделены от дней без товара;
- есть горизонт поставки и нормальный pipeline;
- нет жестких стопов `дубль`, `снятый`, замена, `не закупать`;
- результат BRT потом проходит обычные проверки цены, поставщика, MOQ, бюджета,
  pipeline и `confidence score`.

## `procurement_behavior_profile.v1`

Папка номенклатуры остается первым уровнем. Расчетная единица закупки отвечает
на вопрос "на каком уровне считаем". Профиль закупочного поведения отвечает на
вопрос "как эта расчетная единица обычно закупается". Для автоматических заказов
одной папки мало: внутри одной папки могут быть товары с разным характером.
Поэтому второй рабочий уровень - профиль закупочного поведения (`procurement
behavior profile`), то есть "как этот товар или группа обычно заказывается".

Простыми словами: профиль - это не исключение товара из анализа. Это ярлык
поведения перед формулой. Он говорит программе, считать ли товар как обычный
ходовой дисплей, дорогой быстрый, дорогой медленный, новинку, юный товар,
неликвид, товар с высоким MOQ или товар с ручной ответственностью.

Лучший признак для отбора - не одно свойство, а вычисляемый профиль из свойств
номенклатуры, истории продаж, себестоимости, маржи, поставщика и логистики.

Примеры профилей:

| Профиль | Простое значение | Как влияет на заказ |
| --- | --- | --- |
| `batch_common` | Едет и заказывается вместе с основной папкой | Можно считать по графику папки |
| `display_special` | Дисплеи с разными характерами внутри папки | Нужна отдельная квалификация по модели, цене, спросу, риску |
| `slow_prepare` | Долго готовится у поставщика | Увеличивает горизонт покрытия |
| `slow_logistics` | Долго едет | Увеличивает срок поставки и страховой запас |
| `fast_expensive` | Дорогой, но можно быстро довезти | Держать меньший запас, заказывать чаще |
| `slow_expensive` | Дорогой и долго едет | Ручная проверка, лимит денег, точный прогноз |
| `cheap_flagship_bulk` | Дешевый флагман | Больше партия, реже заказ, смотреть 80-90 дней покрытия |
| `cheap_fast_turnover` | Недорогой и быстро продается | Можно держать больше дней запаса |
| `high_moq_burden` | Поставщик заставляет брать много | Проверять раздувание заказа после MOQ |
| `doc_gate_required` | Нужны документы, например АКБ/ВЭД | Блокировать без документов |
| `fragile_quality_risk` | Хрупкий/часто бывают претензии | Повышать риск, включать контроль качества |
| `new_item_test` | Новинка | Не масштабировать по первой продаже |
| `young_item` | Уже не новинка, но история еще короткая | Можно считать, но с пониженной уверенностью |
| `aging_minimum` | Стареет, но еще не неликвид | Заказывать минимум или ручная проверка |
| `nonliquid_control` | Старый/неликвидный товар | Не закупать автоматически |
| `do_not_order` | Дубль, снятый, отказались, есть замена | Исключать из заказа до формулы |
| `property_supplier_split` | В одной группе разные свойства и поставщики | Считать вместе можно, но заказы поставщику разделять |
| `one_off_demand` | Продажа пришла от одного клиента/контрагента | Не масштабировать как стабильный спрос |
| `broad_retail_demand` | Продажи распылены по магазинам/клиентам | Считать здоровым спросом |

Правила дорогих товаров:

- дорогим считается товар из верхних 25% по цене/себестоимости внутри своей
  рабочей группы, а не всего каталога;
- как только в `Заказе поставщику` появляется строка с дорогим товаром, система
  может присвоить профиль `Дорогой быстрый` или `Дорогой медленный`;
- ответственный за папку может вручную присвоить дорогой профиль при первой
  работе с номенклатурой, если цена/риск видны раньше полной статистики;
- `Дорогой быстрый` - товар можно довезти до 7 дней включительно;
- `Дорогой медленный` - товар едет дольше 7 дней или срок не подтвержден;
- страховой запас по дорогому товару задает ответственный человек. Значение
  должно быть внесено в заказ поставщику, Bitrix-карточку и расчетную витрину до
  дня сдачи товара в cargo.

Рекомендуемые признаки для построения профиля:

| Признак | Откуда брать | Зачем нужен |
| --- | --- | --- |
| Папка номенклатуры | 1С | Первый уровень правил |
| Расчетная единица закупки | расчет сервиса | Понять, считаем SKU, аналог, `предмет + tag`, свойства, поставщика или потребность магазина |
| Вид/тип товара | свойство номенклатуры | Отличать АКБ, дисплей, шлейф, стекло и т.д. |
| `Предмет` и `tag` | 1С/РММЗ/TopControl | Использовать готовые признаки ассортимента, если они заполнены надежно |
| Модель/совместимость/серия | свойства номенклатуры | Отличать массовую и редкую модель |
| Бренд/производитель/качество | свойства номенклатуры | Оценивать спрос, возвраты и риск качества |
| Рамка/без рамки, площадка под IC, верификация | свойства/наименование | Разделять поставщика и риск |
| Слова `дубль`, `снятый`, отказ от бренда/копии | наименование/флаг закупки | Исключить из заказа или отправить на ручное решение |
| Характеристики/свойства карточки номенклатуры | 1С/РММЗ/TopControl | Найти готовые признаки для профиля |
| Состав заказов поставщику за 6 месяцев | 1С/заказы поставщику | Понять, что реально заказывается вместе |
| Срок подготовки поставщика | поставщик/история/evidence | Добавлять дни к горизонту покрытия |
| Срок логистики | маршрут/история/evidence | Разделять быстрый дорогой и медленный дешевый путь |
| Распределение продаж по магазинам | история продаж | Отличать здоровую розничную продажу от единичной сделки |
| Количество контрагентов/клиентов в продажах | история продаж/заказы покупателей | Отличать спрос рынка от покупки одним клиентом |
| Себестоимость/цена закупки | 1С/цены поставщика | Лимитировать дорогие товары |
| Валовая прибыль/маржа | продажи/себестоимость | Выделять флагманы по рентабельности |
| Продажи в штуках | 1С/TopControl | Флагман по количеству |
| Продажи в сумме | 1С/TopControl | Флагман по деньгам |
| Валовая прибыль в сумме | 1С/аналитика | Флагман по прибыли |
| Стабильность спроса | XYZ/история продаж | Страховой запас и ручная проверка |
| Оборачиваемость | продажи и остатки | Min/max запас |
| MOQ/кратность/минимальная сумма | поставщик/evidence | Проверка раздувания заказа |
| Требует документов | свойства/папка/ТН ВЭД | Документальная остановка |
| Хрупкость/риск брака | качество/возвраты | Риск поставки и ручная проверка |

Профиль по дисплеям должен собираться в таком порядке:

1. Сначала жесткие стопы: `не закупать`, `дубль`, `снятый`, есть замена, отказ
   от бренда/копии, проблемы качества.
2. Потом свойства товара: качество/бренд, рамка, площадка под IC, верификация,
   совместимость и поставщик по свойству.
3. Потом деньги: себестоимость, ценовой квартиль, замороженные деньги, риск
   падения закупочной цены.
4. Потом спрос: продажи в штуках, деньгах, прибыли, марже, количество магазинов
   и количество клиентов/контрагентов.
5. Потом путь товара: подготовка поставщика, cargo/логистика, приемка и текущий
   pipeline.

Если первые два шага дают блокер, формула количества все равно может быть
посчитана для объяснения, но автозаказ запрещен.

Задача Омару: утвердить стартовую таблицу профилей для пилота. По каждой
пилотной папке нужно указать:

- какие товары едут общей партией;
- какие товары внутри папки требуют отдельного профиля;
- срок подготовки у поставщика;
- срок логистики;
- какие свойства номенклатуры можно использовать для отбора;
- какие признаки точно блокируют автозаказ;
- где ставим min/max запас в днях, штуках, деньгах или смешанно.

## `procurement_supplier_order_history.v1`

Для дисплеев первый источник профилей - не ручной опрос, а разбор заказов
поставщику за последние 6 месяцев. По составу заказов видно, какие товары
реально заказываются вместе, как быстро поставщик собирает товар и сколько
занимает cargo/логистика/приемка.

Зерно анализа: `заказ поставщику -> строка товара -> SKU`.

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `supplier_order_id` | Заказ поставщику |
| `supplier_order_created_at` | Дата появления/создания заказа поставщику |
| `cargo_handoff_at` | Дата сдачи товара в cargo из заказа поставщику |
| `warehouse_receipt_at` | Дата поступления на склад из подчиненных документов |
| `supplier_id` | Поставщик |
| `folder_id` | Папка номенклатуры, для первого пилота `дисплеи` |
| `sku_ref` | SKU/номенклатура |
| `qty` | Количество в заказе |
| `amount` | Сумма |
| `cost_amount` | Себестоимость/закупочная стоимость |
| `batch_group_key` | Признак партии: какие SKU ехали вместе в одном заказе |
| `property_group_key` | Группа свойств внутри папки, например качество/рамка/верификация дисплея |
| `supplier_split_key` | По какому признаку строку надо отделять в заказ другого поставщика |
| `supplier_prepare_days` | `cargo_handoff_at - supplier_order_created_at` |
| `logistics_receiving_days` | `warehouse_receipt_at - cargo_handoff_at` |
| `total_arrival_days` | `warehouse_receipt_at - supplier_order_created_at` |
| `fast_available_flag` | Быстро доступный товар: подготовка до cargo до 7 дней |
| `price_quartile_by_cost` | Ценовая группа по себестоимости: 1-4 квартиль |
| `sales_qty_class` | Класс по продажам в штуках |
| `sales_amount_class` | Класс по продажам в сумме |
| `sales_store_count` | В скольких магазинах были продажи за анализируемое окно |
| `sales_counterparty_count` | Сколько клиентов/контрагентов купило товар |
| `single_counterparty_sales_share` | Какая доля продаж пришла от одного клиента/контрагента |
| `demand_spread_class` | Спрос широкий розничный или единичный/разовый |
| `receipt_count_3_5_months` | Сколько раз товар поступал за последние 3-5 месяцев |
| `first_receipt_at` | Год рождения товара: первое поступление на склад |
| `item_lifecycle_status` | Новый, юный, обычный, старый, неликвид |
| `do_not_order_flag` | Запрет закупки до формулы |
| `do_not_order_reason` | Почему нельзя закупать: дубль, снят, замена, качество, ручной запрет |
| `pipeline_stage` | Где сейчас товар: в компании, скоро едет, сдан в cargo, у поставщика |

Правила расчета по заказам поставщику:

- `supplier_prepare_days` - это согласование цены плюс сборка товара у
  поставщика. Считается как дата сдачи в cargo минус дата появления заказа
  поставщику.
- `logistics_receiving_days` - это cargo/логистика плюс приемка. Считается как
  дата поступления на склад из подчиненных документов минус дата сдачи в cargo.
- дорогой, но быстро доступный товар - высокий квартиль себестоимости и
  `supplier_prepare_days <= 7`.
- дорогой и долго едет - высокий квартиль себестоимости и высокий
  `logistics_receiving_days` относительно своей группы.
- ценовая градация для дисплеев - разделить себестоимость дисплеев на 4 части
  по квартилям, а не назначать руками.
- анализ продаж для классификации делать регулярным срезом, базово каждые 15
  дней: отдельно по количеству и отдельно по сумме продаж.
- 4 продажи по разным магазинам и клиентам - это более сильный спрос, чем 4
  продажи одному клиенту. Для этого обязательно считать `sales_store_count`,
  `sales_counterparty_count` и `single_counterparty_sales_share`.
- если товар уже заказан, но находится на разном этапе пути, это не один общий
  `incoming_qty`. Pipeline нужно разложить минимум на 4 группы: в компании,
  прибудет около 10 дней, прибудет около 20 дней, сдано в cargo, в обработке у
  поставщика.

График заказов не берем из текущей привычки. Его нужно пересчитать:

```text
расчетный_график_заказа =
оборачиваемость_рубля_по_группе
+ логистика_в_днях
+ доля_группы_в_общих_продажах
+ положенная_доля_группы_в_бюджете_запчастей
```

Смысл простым языком: чем быстрее группа превращает деньги в продажи, чем
дольше едет товар и чем больше доля группы в продажах, тем внимательнее и чаще
нужно пересматривать заказ. Но частота не должна ломать бюджет запчастей:
каждая группа получает свою долю бюджета.

## `procurement_seasonality_profile.v1`

Отдельный контракт для глубокого анализа продаж за последние 3 года. Его задача
не просто показать "продажи выросли/упали", а объяснить, почему формула должна
купить больше, меньше или остановиться.

Зерно анализа: `месяц или неделя -> номенклатурная группа -> магазин -> SKU`.

Важно: существующая витрина `onec_sales_daily_kpi` полезна для KPI продаж, но
не подходит как ядро автозаказа, потому что в ней нет SKU и номенклатурной
группы. Для закупки нужен новый read-only слой по строкам реализации и возврата:

- реализация: `_Document203` + `_Document203_VT4966`;
- возврат: `_Document109` + `_Document109_VT1698`;
- номенклатура: `_Reference62`;
- магазин/склад в строке: `_Reference80`;
- группа товара: родитель/папка номенклатуры `_Reference62._ParentIDRRef`.

Минимальные поля:

| Поле | Значение |
| --- | --- |
| `period_start` | Начало недели/месяца |
| `period_grain` | Месяц или неделя |
| `folder_id` | Номенклатурная группа/папка |
| `store_id` | Магазин/склад продаж, если анализ по точке |
| `active_sku_count` | Сколько SKU реально участвовало в ассортименте периода |
| `selling_sku_count` | Сколько SKU имело продажи |
| `zero_sales_sku_count` | Сколько активных SKU не продавалось |
| `sales_qty` | Продажи в штуках |
| `sales_amount` | Продажи в деньгах |
| `cost_amount` | Себестоимость продаж |
| `gross_profit_amount` | Валовая прибыль |
| `sales_store_count` | Сколько магазинов/точек дали продажи |
| `sales_counterparty_count` | Сколько клиентов/контрагентов дали продажи |
| `single_counterparty_sales_share` | Доля продаж одного крупнейшего клиента/контрагента |
| `demand_spread_class` | Спрос широкий розничный, узкий или разовый |
| `one_off_demand_flag` | Признак разовой продажи, которую нельзя масштабировать как обычный спрос |
| `sales_per_active_sku` | Продажи на одну активную позицию |
| `selling_sku_share` | Доля SKU, которые реально продавались |
| `top_sku_concentration_pct` | Какая доля продаж приходится на топовые SKU |
| `stockout_days` | Дни, когда товар/группа могли не продаваться из-за отсутствия |
| `lost_sales_estimate_qty` | Оценка потерянных продаж из-за отсутствия товара |
| `seasonality_factor_qty` | Сезонный коэффициент по штукам |
| `seasonality_factor_amount` | Сезонный коэффициент по деньгам |
| `seasonality_confidence` | Насколько сезонность надежна по данным |
| `plan_sales_qty` | План продаж в штуках |
| `forecast_sales_qty` | Прогноз продаж в штуках |
| `plan_gap_pct` | Насколько факт/прогноз расходится с планом |
| `store_need_qty` | Потребности магазинов, например `Потребности Савелова` |
| `rmmz_need_qty` | Потребность из РММЗ |
| `topcontrol_need_qty` | Потребность из TopControl |
| `procurement_strengths` | Что в закупке получилось хорошо |
| `procurement_weaknesses` | Где закупка три года проваливалась |
| `risk_flags` | Риски, которые должны менять формулу |

Как считать сезонность простым языком:

1. Берем 36 месяцев продаж по группе, магазину и SKU.
2. Отдельно считаем, сколько SKU было активно в каждом месяце. Если группа
   выросла с 200 до 400 позиций, нельзя сравнивать только общую сумму продаж.
3. Считаем продажи на одну активную позицию и долю SKU, которые реально
   продавались.
4. Проверяем ширину спроса: сколько магазинов, клиентов и заказов дали продажи.
   Разовая покупка одним клиентом не должна превращаться в большой автозаказ.
5. Убираем искажения дефицита: дни без товара не считаем как отсутствие спроса.
6. Сравниваем месяц с нормальным уровнем этой же группы за 3 года.
7. Получаем сезонный коэффициент: например, `1.25` значит "в этот период обычно
   нужно на 25% больше", `0.80` - "обычно на 20% меньше".

Сильные стороны закупок за 3 года ищем так:

- пик сезона закрыт остатком до начала роста продаж;
- мало потерянных продаж из-за отсутствия;
- после сезона нет большого хвоста сверхнорматива;
- флагманы по штукам, деньгам и прибыли были в наличии;
- потребности магазинов закрывались перемещением или закупкой вовремя;
- доля продающихся SKU растет быстрее, чем общий ассортимент.

Слабые стороны закупок за 3 года ищем так:

- группа растет по количеству SKU, но продажи на одну позицию падают;
- сезон начинается раньше, чем приходит товар;
- после сезона остается дорогой или стареющий остаток;
- топовые SKU регулярно заканчиваются, а слабые SKU лежат;
- потребности магазинов повторяются, но не закрываются;
- план продаж выше прогноза, но закупка не готовит запас заранее;
- частые срочные заказы дороже обычной логистики.

Эти выводы не должны оставаться отчетом "для красоты". Они обязаны попадать в
формулу как риски и коэффициенты.

## Расчетная модель 9/10 до пилота

Эта модель сильнее простой формулы. Она разделяет расчет на слои:

0. на каком уровне считать и каким методом спроса;
1. сколько товара реально нужно;
2. сколько товара реально доступно;
3. как сезонность, план продаж и потребности магазинов меняют спрос;
4. какие риски трехлетней истории нужно добавить в расчет;
5. как поставщик ограничивает заказ;
6. можно ли строку пускать в автоматику или надо звать человека.

## Слой 0. Расчетная единица и метод спроса

Перед любой средней продажей программа должна решить два вопроса:

1. на каком уровне считаем потребность;
2. каким методом считаем спрос.

```text
расчетная_единица =
выбрать_уровень(
  папка,
  SKU,
  группа_аналогов,
  предмет + tag,
  свойства_и_характеристики,
  поставщик,
  потребность_магазина,
  ручной_профиль
)
```

```text
метод_спроса =
выбрать_метод(
  стабильность_спроса,
  ширина_спроса,
  доступные_дни_продаж,
  сезонность,
  новизна_товара,
  неликвид,
  потребности_магазинов,
  качество_данных
)
```

Базовые правила:

- стабильный широкий спрос - `available_days_average`;
- сезонный спрос - `seasonal_average`;
- редкий, но подтвержденный спрос - `brt_quantile`;
- заявка магазина/клиента - `store_need` плюс проверка факта продаж;
- новинка, неликвид, конфликт данных или разовая продажа одному клиенту -
  `manual_review`.

Если программа не может уверенно выбрать расчетную единицу или метод спроса,
строка может быть посчитана для объяснения, но автозаказ запрещен.

## Слой 1. Чистый спрос

Главная ошибка простых формул - считать дни без товара как дни без спроса.
Если товара не было, продаж не было не потому, что спрос нулевой, а потому что
продавать было нечего.

Перед расчетом спроса система выбирает папку, расчетную единицу, метод спроса и
профиль закупочного поведения. Например, внутри одной папки часть товаров может
ехать общей партией, часть считается как группа аналогов, часть через
`предмет + tag`, часть через свойства дисплея, а часть требует отдельного
правила из-за цены, срока подготовки, логистики, маржи или риска.

```text
чистая_средняя_продажа =
продажи_за_период
/
дни_когда_товар_был_доступен_в_активных_точках
```

Если выбран `brt_quantile`, обычная средняя не является главным числом. В этом
случае программа строит распределение потребности по историческим интервалам
поставки и выбирает количество под целевой уровень наличия:

```text
BRT_потребность =
квантиль(
  исторические_интервалы_спроса_на_горизонт_поставки,
  целевой_уровень_наличия
)
```

BRT не применяется к разовой продаже одному клиенту, к новинкам без нормальной
истории, к неликвиду и к строкам с жесткими стопами. Если BRT применен, его
результат все равно проходит все следующие слои: сезонность, pipeline, MOQ,
поставщик, цена, риск, документы и подтверждение человека.

В расчет продаж входят только активные торговые склады. Центральный склад,
транзитные склады и склады перемещений не дают дни наличия для средней продажи.

Если доступных дней мало, программа не должна уверенно масштабировать закупку.
Она ставит низкую уверенность (`confidence score`) и отправляет строку на ручную
проверку.

## Слой 2. Горизонт покрытия

Покупать надо не только на срок поставки. Если папка заказывается раз в неделю,
а поставка идет 20 дней, нужно покрыть не 20 дней, а примерно:

```text
горизонт_покрытия =
срок_подготовки_товара_у_поставщика
+
срок_логистики
+
период_между_заказами
+
буфер_задержки_поставщика
+
буфер_приемки
```

Пример: подготовка 5 дней, логистика 15 дней, заказываем раз в 7 дней, обычная
задержка 3 дня, приемка 2 дня:

```text
горизонт_покрытия = 5 + 15 + 7 + 3 + 2 = 32 дня
```

## Слой 3. Целевой остаток

```text
спрос_на_горизонт =
чистая_средняя_продажа
*
горизонт_покрытия
*
коэффициент_тренда
*
коэффициент_сезонности_по_3_годам
*
коэффициент_плана_и_прогноза
*
коэффициент_потребностей_магазинов
*
коэффициент_качества_ассортимента
*
коэффициент_распределения_спроса
*
коэффициент_важности_товара
```

Если тренда, сезонности, плана, прогноза или потребностей магазинов пока нет в
данных, коэффициент равен `1`, но поле в контракте все равно сохраняется. Это
важно, чтобы потом не переделывать модель.

Как применять новые коэффициенты:

- сезонность по 3 годам повышает заказ перед устойчивым пиком и снижает после
  сезона;
- план/прогноз продаж повышает заказ, если утвержденный план выше обычного
  спроса, но снижает уверенность, если план не подтвержден источником;
- потребности магазинов, например `Потребности Савелова`, добавляются как
  отдельный сигнал спроса, но не заменяют факт продаж;
- качество ассортимента снижает автоматическую закупку, если группа раздулась
  по SKU, а продажи на одну активную позицию падают.
- распределение спроса повышает уверенность, если продажи идут по разным
  магазинам/клиентам, и снижает заказ/уверенность, если продажи разовые.

Коэффициент важности товара должен собираться не только по одному ABC, а по
нескольким квалификациям:

- флагман по штукам;
- флагман по сумме продаж;
- флагман по валовой прибыли;
- флагман по себестоимости/замороженным деньгам;
- флагман по рентабельности;
- стратегический товар для наличия;
- товар с высоким риском падения цены.

```text
страховой_запас =
чистая_средняя_продажа
*
дни_страхового_запаса
*
коэффициент_нестабильности_спроса
```

`ABC/XYZ` влияет именно здесь:

- важные и стабильные товары получают выше сервисный уровень;
- нестабильные товары получают больше ручной проверки;
- неликвид не должен получать закупку только потому, что формула дала малое
  положительное число.

```text
целевой_остаток =
спрос_на_горизонт
+
страховой_запас
+
минимальный_остаток_для_наличия/выкладки
```

## Слой 4. Реально доступный товар

В 1С нужно различать фактический и свободный остаток:

- фактический остаток - физически числится на складе;
- свободный остаток - реально доступен для продажи, перемещения или покрытия
  потребности.

Для заказа основа - свободный остаток. Фактический остаток нужен для контроля,
расхождений и понимания, где товар есть физически, но он не свободен.

```text
доступный_товар =
свободный_остаток_в_активных_точках
+
перемещаемый_свободный_остаток_центрального_и_транзитных_складов
+
товар_в_компании_готовый_к_распределению
+
товар_который_придет_примерно_за_10_дней
+
товар_который_придет_примерно_за_20_дней
+
товар_сданный_в_cargo
+
товар_в_обработке_у_поставщика_с_надежной_датой
-
остаток_который_нельзя_перемещать
```

Важно: центральный и транзитные склады уменьшают сетевую потребность, но не
искажают среднюю продажу. Если дефицит есть только в отдельных магазинах, а в
сети товара хватает, действие должно быть `переместить`, а не `купить`.

Если дата прихода ненадежная, такая строка pipeline не должна полностью
уменьшать закупку. Она уменьшает уверенность и показывается человеку отдельно.

## Слой 5. Первичная потребность к закупке

```text
сырая_потребность =
целевой_остаток
-
доступный_товар
```

```text
потребность_к_закупке = max(0, сырая_потребность)
```

Если получилось `0`, программа не создает заказ. Она может создать действие
`переместить`, `не покупать` или `проверить`, если есть риск.

## Слой 6. Ограничения поставщика

```text
количество_после_поставщика =
округлить_вверх(
  потребность_к_закупке,
  MOQ,
  кратность_упаковки,
  минимальная_сумма_заказа
)
```

Если округление делает закупку слишком большой, строка не должна тихо улетать в
заказ. Она уходит на ручную проверку с причиной:
`поставщик заставляет купить больше реальной потребности`.

## Слой 7. Верхние ограничения

После округления проверяем, не получился ли вредный заказ.

```text
финальное_количество =
min(
  количество_после_поставщика,
  максимум_по_правилу_профиля,
  максимум_по_бюджету,
  максимум_по_риску_папки
)
```

Если верхнее ограничение срезало количество, программа должна объяснить это в
`reason_text`.

Min/max запас нельзя всегда считать только в днях. Лучший вариант - гибридное
правило по профилю товара:

| Характер товара | Минимум | Максимум | Почему |
| --- | --- | --- | --- |
| Дешевый быстрый ходовой | дни + минимум выкладки | дни или коробки | Можно держать больше, риск денег ниже |
| Дорогой быстро доступный | минимум наличия/витрины | деньги + короткие дни | Лучше чаще заказывать, меньше замораживать |
| Дорогой долго едет | дни + ручной буфер | деньги + риск + ручная проверка | Ошибка дорогая, но отсутствие тоже опасно |
| Долго готовится у поставщика | дни подготовки + логистика | дни покрытия + лимит денег | Надо заранее, иначе будет разрыв |
| Высокий MOQ | минимум поставщика | максимум по раздуванию | Проверять, не заставляет ли поставщик купить лишнее |
| Нестабильный спрос | маленький минимум | ручная проверка | Формула может ошибаться |
| Новинка | тестовая партия | ручной лимит | Нельзя масштабировать по первой продаже |
| Неликвид | 0 | 0 без ручного решения | Автозаказ запрещен |
| АКБ/ВЭД | только после документов | ручной лимит | Документы и логистика важнее формулы |

Итого min/max должен храниться не одним числом, а набором:

```text
min_stock_rule = max(дни_минимума, минимум_выкладки, минимум_поставщика_если_разрешен)
max_stock_rule = min(дни_максимума, денежный_лимит, лимит_риска, лимит_MOQ_раздувания)
```

## Слой 8. Риски трехлетнего анализа

После обычного расчета программа должна проверить, что закупка не повторяет
старые ошибки.

| Риск | Что значит простым языком | Как влияет на формулу |
| --- | --- | --- |
| `seasonality_miss_risk` | В прошлые годы сезон начинался раньше закупки | Увеличить горизонт/буфер до сезона |
| `stockout_risk` | Продажи могли быть ниже из-за отсутствия товара | Не считать нули как слабый спрос, поднять прогноз |
| `overstock_after_peak_risk` | После сезона оставался лишний остаток | Снизить максимум и запретить автозаказ после пика |
| `assortment_bloat_risk` | SKU стало больше, а продажи на SKU хуже | Снизить автоматический заказ слабых позиций |
| `concentration_risk` | Продажи держатся на малом числе SKU | Флагманы защищать, слабые SKU отправлять на проверку |
| `one_off_demand_risk` | Продажи пришли от одного клиента/заказа | Не масштабировать как стабильный спрос |
| `plan_gap_risk` | План продаж сильно расходится с фактом/прогнозом | Снизить уверенность и показать человеку |
| `store_need_unserved_risk` | Магазины просили товар, но потребность не закрывали | Добавить потребность или предложить перемещение |
| `urgent_logistics_risk` | Часто приходилось везти срочно и дороже | Повысить буфер или пересмотреть график папки |

```text
риск_поправка =
сезонный_риск
+ риск_дефицита
+ риск_раздутого_ассортимента
+ риск_разового_спроса
+ риск_незакрытых_потребностей_магазинов
+ риск_расхождения_с_планом
```

Если риск высокий, программа не обязана уменьшать или увеличивать количество
вслепую. Главное правило: высокий риск снижает `confidence score` и переводит
строку в ручную проверку, если нет надежного объяснения.

## Слой 9. Решение

Формула количества не решает сама, можно ли делать автозаказ. Последний слой -
решение:

| Условие | Решение |
| --- | --- |
| Все данные надежные, риск низкий, поставщик/цена/договор/документы есть | Можно создать черновик автоматически после разрешенного режима |
| Количество понятно, но есть цена/поставщик/документ/скачок/новинка/неликвид | Ручная проверка |
| В сети товара хватает, но он не там | Перемещение |
| Товар запрещен, неликвидный или нет документов по АКБ/ВЭД | Не покупать / блокер |

## Итоговая формула простым языком

```text
К заказу =
сколько продадим до следующей нормальной поставки
* поправки: сезонность за 3 года, план/прогноз продаж,
  потребности магазинов, качество ассортимента,
  распределение спроса по магазинам и клиентам
+
страховой запас
+
минимум для нормального наличия
-
свободный остаток, который реально можно использовать
-
что уже есть/едет по этапам: компания, 10 дней, 20 дней, cargo, поставщик
потом округлить под условия поставщика
потом ограничить по профилю товара: дни, штуки, деньги, риск
потом проверить риски: сезон, дефицит, разовый спрос, раздутый ассортимент,
незакрытые потребности
потом проверить стоп-сигналы
```

Оценка до пилота: `9/10`, если заполнены настройки папки, склады, сроки
поставки, задержки, резервы, упаковка, поставщик, цена и стоп-сигналы.
Без этих данных даже хорошая формула падает обратно до `5-6/10`.

## `procurement_decision_contract.v1`

Строка решения для закупки.

| Поле | Значение |
| --- | --- |
| `run_id` | ID расчета |
| `sku_ref` | Ссылка/код товара из 1С |
| `sku_name` | Название товара |
| `folder_id` | Папка номенклатуры |
| `calculation_unit_id` | ID расчетной единицы закупки |
| `calculation_unit_level` | Уровень расчета: SKU, аналог, `предмет + tag`, группа свойств, поставщик, потребность магазина, ручной профиль |
| `calculation_unit_key` | Стабильный ключ расчетной единицы |
| `calculation_unit_reason` | Почему программа считает именно на этом уровне |
| `behavior_profile_code` | Профиль закупочного поведения внутри папки |
| `profile_source` | Откуда взят профиль: свойство, правило, ручная настройка, история |
| `item_type` | Вид товара: АКБ, дисплей, шлейф и т.д. |
| `item_subject` | `Предмет`, если он используется в расчете |
| `item_tags` | Теги, которые повлияли на профиль или расчет |
| `analog_group_id` | Группа аналогов/замен, если расчет идет не по одиночному SKU |
| `brand_or_quality_class` | Бренд/качество/класс, если влияет на спрос или риск |
| `model_or_compatibility_group` | Модель/совместимость/серия |
| `characteristic_values` | Значимые характеристики товара |
| `display_property_group` | Для дисплеев: InCell/GX/ORIG100/Soft OLED/Hard OLED и т.д. |
| `frame_type` | Рамка/без рамки, если влияет на поставщика или спрос |
| `ic_platform_flag` | Есть ли площадка под IC, если признак нужен для закупки |
| `verification_flag` | Есть ли проверка/верификация, если признак нужен для закупки |
| `duplicate_name_flag` | В названии есть `дубль` или аналогичный стоп-признак |
| `discontinued_flag` | Товар снят/отменен/не поддерживается |
| `replacement_available_flag` | Есть замена или ожидается собственная марка/аналог |
| `do_not_order_flag` | Отдел закупки запретил закупать |
| `do_not_order_reason` | Простая причина запрета закупки |
| `warehouse_scope` | Какие склады вошли в расчет |
| `sales_window_days` | За какой период считали продажи |
| `sales_qty` | Продажи за период |
| `sales_amount` | Продажи в деньгах |
| `gross_profit_amount` | Валовая прибыль в деньгах |
| `gross_margin_pct` | Рентабельность/маржинальность |
| `cost_amount` | Себестоимость/замороженные деньги |
| `three_year_sales_window_months` | Сколько месяцев истории вошло в анализ, базово 36 |
| `active_sku_count_group_period` | Сколько SKU было активно в группе за период |
| `selling_sku_share` | Доля активных SKU, которые реально продавались |
| `zero_sales_sku_share` | Доля активных SKU без продаж |
| `sales_per_active_sku` | Продажи на одну активную позицию |
| `sku_sales_share_in_group` | Доля SKU в продажах своей группы |
| `top_sku_concentration_pct` | Концентрация продаж на топовых SKU |
| `flagship_by_qty` | Флагман по количеству продаж |
| `flagship_by_amount` | Флагман по сумме продаж |
| `flagship_by_profit` | Флагман по валовой прибыли |
| `flagship_by_margin` | Флагман по рентабельности |
| `flagship_by_cost` | Флагман по себестоимости/цене риска |
| `sales_store_count` | Сколько магазинов дали продажи |
| `sales_counterparty_count` | Сколько клиентов/контрагентов дали продажи |
| `single_counterparty_sales_share` | Доля продаж крупнейшего клиента/контрагента |
| `demand_spread_class` | Широкий розничный спрос, узкий спрос или разовая продажа |
| `one_off_demand_flag` | Продажи похожи на разовый спрос |
| `broad_retail_demand_flag` | Продажи распределены по магазинам/клиентам |
| `available_sales_days` | Дни, когда товар был доступен в активных точках |
| `out_of_stock_days` | Дни, когда товара не было и спрос мог быть скрыт |
| `avg_sales_qty` | Средняя продажа |
| `avg_sales_basis` | Как считалась средняя продажа |
| `demand_method_code` | Метод спроса: средняя, сезонная средняя, BRT, потребность магазина, ручная проверка |
| `method_selection_reason` | Почему выбран этот метод спроса |
| `method_confidence` | Уверенность в выбранном методе |
| `brt_history_days` | Сколько дней истории взял BRT |
| `brt_supply_horizon_days` | На какой горизонт BRT оценивал спрос |
| `brt_service_level_target` | Целевой уровень наличия для BRT |
| `brt_candidate_qty` | Количество, предложенное BRT до ограничений |
| `brt_blocker_reason` | Почему BRT не применен, если он был кандидатом |
| `item_created_at` | Дата рождения товара в карточке/программе |
| `first_receipt_at` | Первое поступление на склад |
| `last_receipt_at` | Последнее поступление на склад |
| `receipt_count_recent` | Количество поступлений за последние 3-5 месяцев |
| `item_lifecycle_status` | Новый, юный, обычный, старый, неликвид |
| `trend_factor` | Поправка на рост/падение спроса |
| `seasonality_factor` | Поправка на сезонность по трехлетней истории |
| `seasonality_confidence` | Насколько можно верить сезонности |
| `sales_plan_qty` | План продаж на горизонт расчета |
| `sales_forecast_qty` | Прогноз продаж на горизонт расчета |
| `plan_forecast_factor` | Поправка по плану и прогнозу |
| `plan_gap_pct` | Расхождение плана с прогнозом/фактом |
| `store_need_qty` | Суммарная потребность магазинов |
| `store_need_names` | Какие потребности учтены, например `Потребности Савелова` |
| `rmmz_need_qty` | Потребность из РММЗ |
| `topcontrol_need_qty` | Потребность из TopControl |
| `store_need_factor` | Поправка по потребностям магазинов |
| `assortment_quality_factor` | Поправка на качество ассортимента |
| `importance_factor` | Поправка на важность товара: флагман, AX/AY/BX |
| `demand_confidence` | Уверенность в спросе |
| `actual_stock_sales_points_qty` | Фактический остаток в торговых точках |
| `free_stock_sales_points_qty` | Свободный остаток в торговых точках |
| `actual_stock_central_transit_qty` | Фактический остаток центрального/транзитных складов |
| `free_stock_central_transit_qty` | Свободный остаток центрального/транзитных складов |
| `movable_free_stock_qty` | Свободный остаток, который можно переместить |
| `reserved_qty` | Резервы под клиентов или уже принятые обязательства |
| `blocked_stock_qty` | Брак, карантин, спорный остаток |
| `incoming_qty` | Уже заказано или едет |
| `pipeline_in_company_qty` | Товар уже в компании и может быть распределен |
| `pipeline_arriving_10_days_qty` | Ожидается примерно за 10 дней |
| `pipeline_arriving_20_days_qty` | Ожидается примерно за 20 дней |
| `pipeline_cargo_handoff_qty` | Уже сдано в cargo |
| `pipeline_supplier_processing_qty` | Еще у поставщика в сборке/обработке |
| `pipeline_date_confidence` | Насколько надежны даты прихода по pipeline |
| `supplier_prepare_days` | Сколько дней поставщик готовит товар |
| `logistics_days` | Сколько дней товар едет |
| `lead_time_days` | Общий срок до поступления |
| `order_cadence_days` | Период между заказами |
| `supplier_delay_buffer_days` | Буфер задержки поставщика |
| `receiving_buffer_days` | Буфер приемки |
| `coverage_days` | На сколько дней нужно закрыть спрос |
| `forecast_qty` | Прогноз спроса на срок поставки |
| `safety_stock_qty` | Страховой запас |
| `target_stock_qty` | Целевой остаток |
| `raw_need_qty` | Сырая потребность до округлений |
| `supplier_rounded_qty` | Количество после MOQ/кратности/минимальной суммы |
| `recommended_order_qty` | Финально сколько купить |
| `min_stock_rule_type` | Как считается минимум: дни, штуки, деньги, смешанно |
| `min_stock_qty` | Минимальный запас в штуках после пересчета |
| `max_stock_rule_type` | Как считается максимум: дни, штуки, деньги, риск, смешанно |
| `max_stock_cap_qty` | Верхнее ограничение по запасу |
| `budget_cap_qty` | Верхнее ограничение по бюджету, если применяется |
| `moq_overbuy_ratio` | Насколько MOQ раздувает заказ относительно реальной потребности |
| `seasonality_miss_risk` | Риск не успеть к сезонному пику |
| `stockout_risk` | Риск, что продажи занижены из-за отсутствия товара |
| `overstock_after_peak_risk` | Риск лишнего остатка после сезона |
| `assortment_bloat_risk` | Риск раздутого ассортимента без продаж |
| `concentration_risk` | Риск, что продажи держатся на малом числе SKU |
| `one_off_demand_risk` | Риск разовой продажи одним клиентом/заказом |
| `plan_gap_risk` | Риск расхождения с планом продаж |
| `store_need_unserved_risk` | Риск незакрытых потребностей магазинов |
| `urgent_logistics_risk` | Риск частых срочных дорогих поставок |
| `recommended_action` | Купить, переместить, не покупать, проверить |
| `supplier_candidate_id` | Поставщик-кандидат |
| `supplier_resolution_status` | Однозначно найден, не найден, дубль, конфликт |
| `supplier_lead_time_confidence` | Насколько надежен срок поставки |
| `confirmed_price` | Подтвержденная цена |
| `currency` | Валюта |
| `price_evidence_id` | Доказательство цены |
| `price_change_pct` | Изменение цены |
| `price_jump_flag` | Есть скачок больше/меньше порога |
| `supplier_price_after_request` | Новая цена поставщика после запроса/сверки заказа |
| `supplier_price_request_signal` | Сигнал: поставщик резко изменил цену после запроса |
| `moq_qty` | Минимальное количество заказа |
| `package_multiple_qty` | Кратность упаковки |
| `min_supplier_order_amount` | Минимальная сумма заказа поставщика |
| `document_gate_status` | Не требуется, не готово, готово |
| `blockers` | Причины, почему нельзя автоматом |
| `warnings` | Предупреждения, которые не блокируют, но должны быть видны |
| `formula_version` | Версия формулы |
| `confidence_score` | Общая уверенность программы по строке |
| `auto_order_allowed` | Можно ли пускать в автоматический черновик |
| `reason_text` | Объяснение простым языком |
| `risk_level` | Низкий, средний, высокий |
| `approval_status` | Ожидает, подтверждено, отклонено |

## `procurement_supplier_resolution.v1`

Правило безопасного выбора поставщика.

Поставщик может быть выбран автоматически только если:

- найден ровно один кандидат;
- он связан с CRM-компанией Bitrix и контрагентом 1С;
- нет дублей;
- договор, валюта, склад прихода и условия оплаты определены;
- цена свежая и подтверждена evidence;
- нет скачка цены больше/меньше порога;
- поставщик разрешен для папки номенклатуры;
- нет блокера по качеству, документам или спорной поставке.

## `procurement_onec_file_exchange.v1`

Файл обмена в 1С должен создавать только непроведенный черновик.

Минимальные поля:

- поставщик 1С;
- договор;
- валюта;
- склад прихода;
- дата заказа;
- контур закупки: `Обычный`, `Cargo`, `ВЭД импорт`;
- строки товара: номенклатура, количество, цена, валюта, комментарий;
- ссылка на Bitrix-карточку закупки;
- ID подтверждения человеком;
- ID расчета;
- признак `draft_only=true`.

# Invariants

- Нельзя проводить документ 1С автоматически.
- Нельзя писать напрямую в SQL 1С.
- Нельзя создавать заказ без подтверждения человеком.
- Нельзя автоматически выбирать поставщика при дублях или конфликте.
- Нельзя считать центральный склад торговой точкой для средней продажи.
  Торговыми точками считаются только склады, откуда товар продается
  систематически; склады брака и склады с несистемными продажами должны быть
  исключены отдельной настройкой 1С.
- Нельзя закупать позицию с признаком `не закупать`.
- Нельзя автоматически закупать позицию со словом `дубль`, `снятый`, с
  подтвержденной заменой или ожидаемой собственной маркой без ручного решения.
- Нельзя масштабировать разовую продажу одному клиенту как широкий розничный
  спрос.
- Нельзя двигать АКБ/ВЭД дальше без документальной остановки.
- Все сложные термины в интерфейсе и документации должны иметь простую
  расшифровку рядом.

# Errors / Edge Cases

- Нет продаж, но есть стратегический товар: строка уходит на ручную проверку.
- Мало доступных дней продаж: расчет показывается, но уверенность низкая и
  автозаказ запрещен.
- Профиль закупочного поведения не определен: использовать базовое правило
  папки, но автозаказ запретить до классификации.
- Есть продажи, но весь остаток на центральном складе: не искажать среднюю
  продажу, отдельно показать потребность перемещения.
- Дефицит можно закрыть перемещением: рекомендовать перемещение до закупки.
- MOQ или коробка заставляют купить слишком много: строка уходит на ручную
  проверку.
- Срок поставки поставщика неизвестен или устарел: использовать ручную настройку
  папки и снизить confidence score.
- Фактический остаток есть, но свободного остатка нет: не считать товар
  доступным для заказа/перемещения, показать причину.
- Товар дорогой, но быстро доступный: не держать большой запас только потому,
  что он важный; чаще заказывать меньшими партиями.
- Товар дорогой и долго едет: включать ручную проверку или отдельный лимит
  денег, даже если спрос высокий.
- Для дорогого товара страховку задает ответственный человек. Значение нужно
  записать везде, где оно нужно для решения: в заказе поставщику, Bitrix-карточке
  и расчетной витрине, крайний срок - до дня сдачи товара в cargo.
- Основной поставщик есть в РММЗ, но нет цены или договора: ручная проверка.
- Цена изменилась больше/меньше 10% за два заказа: ручная проверка.
- Поставщик после запроса/сверки заказа дал новую цену с сильным изменением:
  ручная проверка, даже если старая цена была нормальная.
- В помощнике закупок строка высоко отсортирована по `К ЗАКАЗУ`: это повод
  смотреть первой, но не разрешение закупать без стоп-проверок.
- В названии есть `дубль`: ставить блокер `do_not_order`, пока закупка явно не
  разрешит исключение.
- Товар `снятый`, есть замена или ожидается собственная марка: ручная проверка
  или `не закупать`, даже если есть расчетный спрос.
- Товар еще ни разу не поступал на склад: считать новой позицией без нормальной
  истории продаж.
- Товар поступил до 3 раз за последние 3-5 месяцев: считать новой тестируемой
  позицией и не масштабировать по первой продаже.
- Товар поступил больше 3 раз: уже не новинка, а `юный` товар; считать можно,
  но с пониженной уверенностью до накопления истории.
- Товар старше года и продан меньше 3 штук за последние 6 месяцев: считать
  кандидатом в неликвид и не закупать автоматом.
- Товар старше года и продан 3 штуки или меньше за 180 дней: считать
  неликвидным кандидатом, создать задачу на цену/остаток, не закупать автоматом.
- Продано 4 штуки одному клиенту/контрагенту: не считать широким спросом.
- Продано 4 штуки по нескольким магазинам/клиентам: считать более сильным
  сигналом, но все равно проверять наличие и цену.
- Дорогой товар: как только строка дорогого товара появилась в заказе
  поставщику, присваивать профиль `Дорогой быстрый` или `Дорогой медленный`;
  при первой работе с номенклатурой ответственный за папку может поставить
  профиль вручную.
- Дорогой товар без подтвержденного срока маршрута считать `Дорогой медленный`
  до уточнения ответственным.
- Дешевый флагман по дисплеям: можно рассматривать большую партию и покрытие до
  80-90 дней, если спрос широкий, цена стабильная и нет риска неликвида.
- Дата прихода по pipeline ненадежная: не вычитать весь товар из потребности
  вслепую, снизить уверенность и показать человеку.
- Количество SKU в группе выросло, а продажи на SKU упали: не масштабировать
  закупку только по общей сумме продаж.
- Потребности магазинов есть, но товара хватает в сети: сначала предложить
  перемещение, потом закупку.
- План продаж выше прогноза, но источник плана не подтвержден: снизить
  уверенность и показать человеку.
- Сезонный коэффициент высокий, но данных меньше 2-3 сезонов: не пускать
  строку в автозаказ без ручной проверки.
- WeChat evidence есть только в переписке: прикрепить файл/скрин/ссылку в
  Bitrix перед автоматикой.
- АКБ без MSDS/UN38.3/сертификата/ТН ВЭД/invoice/packing list: документальная
  остановка.

# Дорожная карта

## Выполненный слой v1

- [x] Добавить чистый сервис `app/services/assortment_lifecycle.py` для расчета
      статуса ассортимента по событиям без БД-миграции и без проведения заказов.
- [x] Реализовать переходы `Плод -> Новорожденный -> ПН -> Новинка -> СП ->
      ПРОДАЖА -> Рабочий`.
- [x] Реализовать ручной `Эксклюзив`: причина, утвердивший, дата, ручной
      минимальный остаток, пересмотр через 30 дней.
- [x] Реализовать профиль дорогого товара только как `Дорогой быстрый` /
      `Дорогой медленный`.
- [x] Реализовать фильтр системных точек продаж: исключить центральный склад,
      транзит, брак и несистемные продажи.
- [x] Расширить `nomenclature_property_updates.v1` для передачи статуса,
      профиля закупочного поведения, минимального остатка эксклюзива и даты
      пересмотра.
- [x] Добавить dry-run task `tasks/build_assortment_lifecycle_updates.py`,
      который превращает нормализованные факты по товарам в строки
      `nomenclature_property_updates.v1` без записи в 1С.
- [x] Добавить read-only builder
      `tasks/build_assortment_lifecycle_facts.py` для пилота `дисплеи`: он
      собирает `assortment_lifecycle_facts.v1` из фактов 1С или offline fixture,
      требует складскую политику, останавливается на неподтвержденном SQL-маппинге
      поступлений и не пишет в 1С/Bitrix.

## Этап 0. Инвентаризация и правила папок

Цель: понять, какие данные реально доступны и какие папки запускаются первыми.

Результат:

- первый пилот: `дисплеи`, ответственный Омар Бочаров;
- `АКБ` остается отдельной рискованной веткой для следующего пилота после
  настройки документальной остановки;
- заполнен `procurement_folder_config.v1`;
- заполнен стартовый `procurement_calculation_unit.v1`: где считаем SKU, где
  аналоги, где `предмет + tag`, где свойства, где потребность магазина;
- заполнен `procurement_demand_method_policy.v1`: где средняя, где сезонность,
  где BRT, где ручная проверка;
- заполнен стартовый `procurement_behavior_profile.v1`;
- разобраны заказы поставщику по дисплеям за последние 6 месяцев;
- построены фактические партии: что заказывалось и ехало вместе;
- определены активные торговые склады и склады только для остатка;
- подтверждено, где в 1С брать свободные и фактические остатки;
- подтвержден список поставщиков-кандидатов;
- утвержден пакет документов для АКБ;
- понятен формат файлового обмена с 1С.

Критерий готовности: можно запустить расчет read-only и объяснить каждую строку.

## Этап 0.1. Паспорт ассортимента и подготовка к 1С КА

Цель: построить классификацию товаров, которая подходит и для текущего
read-only расчета, и для будущего типового модуля управления ассортиментом в
`1С КА`.

Результат:

- сформирован `assortment_passport.v1` по товарам внутри `ОБЩИЙ КАТАЛОГ`;
- проверено покрытие ключевых признаков: папка, `Вид номенклатуры`, `Предмет`,
  категория, совместимость с брендом/моделью, качество, важные характеристики;
- выделены признаки, которые уже можно использовать в расчете, и признаки,
  которые требуют ручной нормализации;
- рассчитан `data_quality_score` по товару и группе;
- предложен стартовый `assortment_status`: матричный, рабочий, новинка, тест,
  под заказ, неликвид, снят, не закупать;
- построены ценовые сегменты внутри узких категорий, например для дисплеев;
- подготовлены рекомендации `assortment_policy_recommendation.v1`: оставить,
  ввести, тестировать, вывести, перевести под заказ или запретить закупку;
- подготовлен файловый пакет `nomenclature_property_updates.v1` для проверки
  и применения свойств номенклатуры в `УТ 10.3` через существующий обмен;
- для каждого поля отмечено, готово ли оно к будущему переносу в `1С КА`.

Критерий готовности: закупка видит не только "сколько купить", но и "должен ли
товар быть в ассортименте"; будущий переход в `1С КА` не требует заново
разбирать справочник.

## Этап 0.2. Разбор заказов поставщику по дисплеям

Цель: получить фактические профили дисплеев из документов, а не из догадок.

Результат:

- выбраны заказы поставщику по дисплеям за последние 6 месяцев;
- по каждому заказу разобран состав товаров;
- рассчитан срок `заказ -> cargo`;
- рассчитан срок `cargo -> поступление на склад`;
- выделены товары, которые обычно заказываются и едут вместе;
- выделены группы, где расчет лучше делать не по папке, а по свойствам,
  характеристикам, `предмет + tag`, аналогам или поставщику;
- себестоимость дисплеев разделена на 4 ценовые группы;
- продажи классифицированы каждые 15 дней отдельно по штукам и по сумме;
- по каждой позиции посчитана ширина спроса: магазины, клиенты/контрагенты,
  доля одного крупнейшего клиента;
- выявлены позиции `дубль`, `снятый`, с заменой, с ручным флагом `не закупать`;
- pipeline по открытым заказам разложен на этапы: в компании, около 10 дней,
  около 20 дней, сдано в cargo, в обработке у поставщика;
- найден список дорогих быстрых, дорогих медленных, новых, юных и неликвидных
  дисплеев.
- найден список дисплеев с редким, но реальным спросом, где нужно сравнить BRT
  с обычной средней.

Критерий готовности: для дисплеев можно объяснить профиль товара документом
поставщика, датами движения и продажами, а не ручным мнением.

## Этап 0.5. Трехлетняя сезонность и качество закупки

Цель: до автозаказа понять, где закупка за 3 года была сильной, где слабой и
какие риски надо добавить в формулу.

Результат:

- выгрузка 36 месяцев продаж на уровне `SKU -> группа -> магазин -> период`;
- количество активной номенклатуры по каждой группе и периоду;
- продажи на одну активную позицию и доля SKU с продажами;
- сезонные коэффициенты по группам;
- план продаж и прогноз продаж по группе/магазину, если источник подтвержден;
- потребности магазинов из РММЗ/TopControl, включая поля вида `Потребности
  Савелова`;
- список сильных и слабых мест закупки по каждой пилотной группе;
- risk flags, которые попадают в `procurement_decision_contract.v1`.

Критерий готовности: формула показывает не только "среднюю продажу", а объясняет
сезон, ассортимент, план, потребности магазинов и риски прошлых закупок.

## Этап 1. Read-only расчет и короткий список

Цель: программа считает, но еще ничего не создает в 1С.

Результат:

- для первого пилота `дисплеи` сформирован
  `assortment_lifecycle_facts.v1` из реальных фактов 1С;
- перед чтением поступлений проходит schema gate по таблице документа и
  табличной части; если маппинг не подтвержден, запуск останавливается с
  `receipt_mapping_unresolved`;
- складская политика пилота вынесена в обязательный JSON-конфиг: системные
  точки продаж, центральный склад, брак, транзит и несистемные продажи;
- ручные `Эксклюзив`, минимальный остаток, подтверждение `Рабочий` и дорогой
  профиль приходят только из override JSON;
- сигналы менеджеров подключаются как optional overlay JSON и пока не считают
  бонусы/KPI;
- `assortment_lifecycle_facts.v1` передается в
  `tasks/build_assortment_lifecycle_updates.py`, который строит
  `nomenclature_property_updates.v1`;
- первый обмен с `Ekama_Test_Arsen` идет только в `dry_run`;
- расчет `recommended_order_qty`;
- выбранная расчетная единица по каждой строке;
- выбранный метод спроса по каждой строке;
- профиль закупочного поведения по каждой строке;
- причины решения по каждой строке;
- статусы: купить, переместить, не покупать, проверить;
- стоп-сигналы;
- отчет по ошибкам данных.

Критерий готовности: отдел закупки может сверить рекомендации с ручным расчетом
и указать расхождения.

## Этап 2. Evidence и поставщик

Цель: связать расчет с поставщиком, ценой и доказательством.

Результат:

- безопасный resolver поставщика;
- история подтвержденных цен;
- ссылка на evidence из WeChat/Disk/файла;
- подсветка скачка цены;
- ручная проверка при дублях, конфликте или отсутствии цены.

Критерий готовности: строка без цены, поставщика или evidence не проходит к
черновику.

## Этап 3. Bitrix-карточка закупки

Цель: дать закупке рабочую очередь решений.

Результат:

- карточка `Закупка/Заказ`;
- контуры `Обычный`, `Cargo`, `ВЭД импорт`;
- стадии и ответственные;
- поля расчета, поставщика, цены, документов, блокеров;
- действия: подтвердить, изменить, запретить, отправить на разбор.

Критерий готовности: закупщик видит не таблицу, а очередь решений.

## Этап 4. Черновик 1С через файловый обмен

Цель: после подтверждения создать непроведенный черновик `ЗаказПоставщику`.

Результат:

- файл обмена `procurement_onec_file_exchange.v1`;
- импорт в 1С в режиме черновика;
- обратная ссылка из Bitrix на документ 1С;
- журнал ошибок импорта.

Критерий готовности: черновик создается только после подтверждения Омаром
Бочаровым и не проводится автоматически.

## Этап 5. АКБ и документальная остановка

Цель: проверить самый рискованный контур на маленькой партии.

Результат:

- АКБ не проходит дальше без пакета документов;
- видно, каких документов не хватает;
- ответственный понимает, что запросить у поставщика;
- есть связь партии, поставщика, документов и закупки.

Критерий готовности: система не дает создать опасный заказ АКБ без документов.

## Этап 6. Боевой пилот 30 дней

Цель: подтвердить, что контур помогает, а не мешает.

Результат:

- минимум одна папка работает регулярно;
- есть статистика ручных проверок;
- есть примеры предотвращенных ошибок;
- известны доработки перед расширением на другие папки.

Критерий готовности: закупка готова расширять контур на следующую папку.

# Acceptance Criteria

- [ ] Для пилотных папок заполнен `procurement_folder_config.v1`.
- [ ] Для пилотных папок заполнен `procurement_calculation_unit.v1`.
- [ ] Для пилотных папок заполнен `procurement_demand_method_policy.v1`.
- [ ] Для пилотных папок заполнен стартовый `procurement_behavior_profile.v1`.
- [ ] Для пилотных папок построен `assortment_passport.v1`.
- [ ] Для пилотных папок сформированы рекомендации
      `assortment_policy_recommendation.v1`.
- [ ] У каждой позиции есть проверка готовности к будущему переносу в `1С КА`:
      категория, статус, матрица, ввод/вывод или блокер.
- [ ] Папка номенклатуры используется как верхний уровень управления, но не как
      единственный уровень формулы.
- [ ] Система умеет считать по SKU, группе аналогов, `предмет + tag`, группе
      свойств, поставщику, потребности магазина или ручному профилю.
- [ ] У каждой строки есть профиль закупочного поведения или блокер "профиль не
      определен".
- [ ] У каждой строки есть выбранный метод спроса или блокер "метод не
      определен".
- [ ] Первый пилот ограничен дисплеями и ответственностью Омара Бочарова.
- [ ] Заказы поставщику по дисплеям за последние 6 месяцев разобраны по составу
      и датам.
- [ ] Помощник закупок используется как источник сигналов: `К ЗАКАЗУ`, история
      поступлений, история продаж, история заказов, текущие остатки, товары к
      получению и к передаче.
- [ ] Срок подготовки поставщика считается как `дата сдачи в cargo - дата
      появления заказа поставщику`.
- [ ] Срок логистики/приемки считается как `дата поступления на склад - дата
      сдачи в cargo`.
- [ ] График заказа пересчитан, а не взят из текущей привычки.
- [ ] Срок до поступления разбит на подготовку товара у поставщика и логистику.
- [ ] В расчете различаются свободные и фактические остатки 1С.
- [ ] Расчет не смешивает торговые склады и склады только для остатка.
- [ ] Средняя продажа считается по доступным дням продаж, а не по календарным
      дням с нулем при отсутствии товара.
- [ ] Для редкого, но реального спроса поддержан метод BRT.
- [ ] BRT не применяется к разовой продаже одному клиенту, новинке без истории,
      неликвиду или строке с жестким стопом.
- [ ] Построен трехлетний анализ продаж по номенклатурным группам.
- [ ] По каждому периоду учтено количество активной номенклатуры в группе.
- [ ] Формула видит продажи на одну активную позицию и долю SKU с продажами.
- [ ] Формула отличает широкий спрос по магазинам/клиентам от разовой продажи
      одному клиенту.
- [ ] Сезонный коэффициент считается отдельно по группе и имеет уровень
      уверенности.
- [ ] План продаж и прогноз продаж входят в расчет как отдельные поля.
- [ ] Потребности магазинов из РММЗ/TopControl, включая `Потребности <магазин>`,
      входят в расчет как отдельный сигнал спроса.
- [ ] Сильные и слабые стороны закупки за 3 года превращаются в risk flags.
- [ ] Горизонт покрытия включает срок поставки, период между заказами, буфер
      задержки поставщика и буфер приемки.
- [ ] Каждая рекомендация формируется в `procurement_decision_contract.v1`.
- [ ] У каждой строки есть `reason_text` простым языком.
- [ ] `reason_text` объясняет выбранную расчетную единицу и метод спроса.
- [ ] У каждой строки есть `confidence_score` и понятное решение:
      автозаказ, ручная проверка, перемещение или не покупать.
- [ ] MOQ, кратность упаковки и минимальная сумма заказа применяются после
      расчета потребности.
- [ ] Если MOQ/кратность раздувают заказ выше разумного остатка, строка уходит
      на ручную проверку.
- [ ] Флагманские признаки считаются минимум по количеству, сумме продаж,
      валовой прибыли, рентабельности и себестоимости/цене риска.
- [ ] Min/max запас поддерживает правила в днях, штуках, деньгах и смешанном
      режиме по профилю товара.
- [ ] Строки с блокерами не попадают в автосоздание черновика.
- [ ] Строки `дубль`, `снятый`, с заменой или ручным флагом `не закупать`
      исключаются до автозаказа.
- [ ] Pipeline открытых заказов разложен по этапам, а не хранится одним общим
      `incoming_qty`.
- [ ] Основной поставщик из РММЗ используется только как кандидат.
- [ ] Скачок цены больше/меньше 10% за два заказа отправляет строку на проверку.
- [ ] `АКБ без документов` блокируется документальной остановкой.
- [ ] Черновик 1С создается только после подтверждения человеком.
- [ ] В Bitrix видны расчет, доказательства, подтверждение и ссылка на 1С.

# Implementation Checklist

- [ ] Создать модель/таблицу запуска расчета закупки.
- [ ] Создать модель/таблицу строки решения закупки.
- [ ] Создать модель/таблицу настроек папки номенклатуры.
- [ ] Создать модель/таблицу расчетных единиц закупки.
- [ ] Создать модель/таблицу политики выбора метода спроса.
- [ ] Создать модель/таблицу профилей закупочного поведения.
- [ ] Реализовать выбор расчетной единицы: SKU, аналог, `предмет + tag`, группа
      свойств, поставщик, потребность магазина, ручной профиль.
- [ ] Реализовать автовыбор метода спроса: средняя, сезонная средняя, BRT,
      потребность магазина, ручная проверка.
- [ ] Реализовать классификатор профиля по папке, свойствам номенклатуры,
      продажам, себестоимости, марже, поставщику и логистике.
- [ ] Реализовать чтение/использование `предмет`, `tag`, характеристик и групп
      аналогов, если они заполнены достаточно надежно.
- [ ] Реализовать анализ заказов поставщику за 6 месяцев для дисплеев.
- [ ] Реализовать чтение сигналов помощника закупок: `К ЗАКАЗУ`, заказано,
      средние продажи, `ДатаМин`, `ДатаМакс`, `Дата рождения`, истории и остатки.
- [ ] Реализовать расчет `supplier_prepare_days` по дате заказа и дате сдачи в
      cargo.
- [ ] Реализовать расчет `logistics_receiving_days` по дате сдачи в cargo и
      дате поступления на склад из подчиненных документов.
- [ ] Реализовать расчет графика заказа от оборачиваемости рубля, логистики,
      доли группы в продажах и доли группы в бюджете запчастей.
- [ ] Реализовать деление дисплеев на 4 ценовые группы по себестоимости.
- [ ] Реализовать классификацию каждые 15 дней по продажам в штуках и сумме.
- [ ] Реализовать расчет ширины спроса по магазинам, клиентам/контрагентам и
      доле крупнейшего клиента.
- [ ] Реализовать стопы по `дубль`, `снятый`, замене, входящей собственной марке
      и ручному флагу `не закупать`.
- [ ] Реализовать pipeline открытых заказов поставщику по этапам.
- [ ] Создать модель/таблицу evidence для цены, срока и документов.
- [ ] Добавить хранение версии формулы и входных параметров расчета.
- [ ] Импортировать/читать из 1С свободные и фактические остатки отдельно.
- [ ] Реализовать расчет средней продажи без искажения центральным/транзитным
      складом.
- [ ] Реализовать BRT для редкого подтвержденного спроса с целевым уровнем
      наличия и объяснением результата.
- [ ] Реализовать доступные дни продаж и дни без товара.
- [ ] Реализовать витрину трехлетних продаж на уровне SKU, группы, магазина и
      периода.
- [ ] Реализовать расчет активной номенклатуры по группе и периоду.
- [ ] Реализовать сезонные коэффициенты по группам с учетом дефицита.
- [ ] Реализовать загрузку/чтение плана продаж и прогноза продаж.
- [ ] Реализовать загрузку/чтение потребностей магазинов из РММЗ/TopControl,
      включая поля вида `Потребности Савелова`.
- [ ] Реализовать risk flags по сильным/слабым местам закупки за 3 года.
- [ ] Реализовать горизонт покрытия:
      `supplier_prepare_days + logistics_days + order_cadence +
      supplier_delay_buffer + receiving_buffer`.
- [ ] Реализовать целевой остаток:
      спрос на горизонт + страховой запас + минимум наличия.
- [ ] Реализовать min/max запас по профилю: дни, штуки, деньги, риск, смешанно.
- [ ] Реализовать флагманские квалификации по количеству, сумме продаж,
      валовой прибыли, рентабельности и себестоимости.
- [ ] Реализовать проверку товара в пути и будущего прихода.
- [ ] Реализовать учет резервов, брака, карантина и заблокированного остатка.
- [ ] Реализовать рекомендацию перемещения перед закупкой.
- [ ] Реализовать MOQ, кратность упаковки, минимальную сумму заказа и проверку
      раздутого заказа после округления.
- [ ] Реализовать стоп-сигналы.
- [ ] Реализовать `confidence_score` и правила автозаказа только для зеленых
      строк.
- [ ] Расширить supplier resolver правилами этого ТЗ.
- [ ] Добавить экспорт файла обмена для 1С в режиме `draft_only`.
- [ ] Добавить создание/обновление Bitrix-карточки закупки.
- [ ] Добавить отчет ручных проверок и блокеров.
- [ ] Добавить документацию для закупки простым языком.

# Tests

- Unit: `supplier_prepare_days` считается как дата сдачи в cargo минус дата
  появления заказа поставщику.
- Unit: `logistics_receiving_days` считается как дата поступления на склад минус
  дата сдачи в cargo.
- Unit: дорогой быстрый дисплей определяется через высокий квартиль
  себестоимости и подготовку до cargo до 7 дней.
- Unit: дорогой медленный дисплей определяется через высокий квартиль
  себестоимости и срок подготовки/логистики от 7 дней.
- Unit: высокий квартиль дорогих товаров для пилота - верхние 25% себестоимости
  внутри своей группы.
- Unit: ценовая группа дисплея считается через 4 квартиля себестоимости.
- Unit: строка с `дубль` получает `do_not_order` до расчета автозаказа.
- Unit: строка с ручным флагом `не закупать` исключается до автозаказа.
- Unit: график заказа меняется при изменении оборачиваемости рубля, логистики,
  доли группы в продажах или доли бюджета.
- Unit: товар без поступлений на склад считается новой позицией.
- Unit: `Плод` - номенклатура создана, но первого заказа поставщику еще нет.
- Unit: `Новорожденный` - появился первый заказ поставщику.
- Unit: `ПН / Потребность новорожденного` появляется при сигнале спроса после
  первого заказа поставщику.
- Unit: `Новинка` начинается от первой сдачи заказа поставщику в cargo.
- Unit: `СП / Старт продаж` начинается от второй сдачи заказа поставщику в cargo.
- Unit: `ПРОДАЖА` начинается от участия товара в документе `Поступление товаров`
  после этапа `СП`.
- Unit: каждое участие товара в документе `Поступление товаров` считается
  поступлением для правила 5 поступлений за 180 дней.
- Unit: `Рабочий` рекомендуется, когда за 180 дней с даты `Новинка` было 5
  поступлений; подтверждает ответственный за папку.
- Unit: `Эксклюзив` не считается готовым без причины, утвердившего, даты,
  ручного минимального остатка и даты пересмотра.
- Unit: `Эксклюзив` формирует строки 1С для статуса, причины, даты, источника,
  утвердившего, минимального остатка и даты пересмотра.
- Unit: персональный сигнал менеджера по новинке хранит источник, количество и
  помечает подозрительно завышенные количества для будущего контроля.
- Unit: товар старше года и с продажами меньше 3 штук за 6 месяцев получает
  риск неликвида.
- Unit: расчет средней продажи по активным торговым складам.
- Unit: расчетная единица выбирается как `subject_tag`, если `предмет` и `tag`
  заполнены надежно.
- Unit: расчетная единица выбирается как `property_group`, если свойства дисплея
  определяют поставщика или риск.
- Unit: расчетная единица выбирается как `analog_group`, если SKU
  взаимозаменяемы.
- Unit: строка с плохими свойствами расчетной единицы получает низкий
  `unit_source_confidence`.
- Unit: стабильный широкий спрос выбирает `available_days_average`.
- Unit: редкий, но широкий спрос выбирает `brt_quantile`.
- Unit: разовая продажа одному клиенту не выбирает BRT и уходит в ручную
  проверку.
- Unit: BRT не применяется к новинке без нормальной истории.
- Unit: BRT не применяется к неликвиду.
- Unit: BRT-кандидат после расчета все равно проходит MOQ, pipeline, цену,
  поставщика и стоп-сигналы.
- Unit: сезонный коэффициент не растет от простого увеличения числа SKU в
  группе.
- Unit: продажи на одну активную позицию падают - формула снижает уверенность.
- Unit: потребность магазина добавляется как сигнал спроса, но не заменяет факт
  продаж.
- Unit: план продаж выше прогноза - строка получает `plan_gap_risk`.
- Unit: высокий `overstock_after_peak_risk` ограничивает максимум после сезона.
- Unit: регулярный дефицит в сезон повышает прогноз, а не занижает спрос.
- Unit: 4 продажи одному клиенту дают `one_off_demand_risk`.
- Unit: 4 продажи по разным магазинам/клиентам повышают `demand_confidence`.
- Unit: строка без профиля закупочного поведения не допускается в автозаказ.
- Unit: профиль `fast_expensive` ограничивает максимум деньгами/короткими днями
  запаса.
- Unit: профиль `slow_prepare` увеличивает горизонт покрытия на дни подготовки.
- Unit: дни без товара не считаются как нулевая продажа.
- Unit: `assortment_passport.v1` берет `Предмет` из надежного объектного
  свойства 1С, а не из неполного плоского свойства.
- Unit: новинка не попадает в обычный ABC/XYZ без отдельного статуса
  `new_item`.
- Unit: товар `on_demand` не смешивается с обычной матрицей и средней продажей.
- Unit: `assortment_policy_recommendation.v1` помечает дорогую новинку как
  требующую ручного утверждения.
- Unit: ценовой сегмент считается внутри узкой категории, а не по всему
  каталогу.
- Unit: центральный/транзитный склад влияет на остаток, но не на среднюю продажу.
- Unit: горизонт покрытия включает срок поставки, период между заказами, буфер
  задержки и буфер приемки.
- Unit: свободный остаток используется для доступности, фактический - для
  контроля и объяснения.
- Unit: флагман по штукам, сумме, прибыли, марже и себестоимости может давать
  разные профили.
- Unit: min/max запас в днях, штуках, деньгах и смешанном режиме дает разные
  ограничения для разных профилей.
- Unit: MOQ/кратность округляет заказ вверх.
- Unit: раздутый заказ после MOQ отправляется на ручную проверку.
- Unit: резервы и заблокированный остаток уменьшают доступный товар.
- Unit: низкий `confidence_score` запрещает автозаказ.
- Unit: скачок цены больше/меньше 10% за два заказа ставит блокер.
- Unit: новая цена поставщика после запроса заказа с сильным изменением ставит
  сигнал ручной проверки.
- Unit: pipeline с ненадежной датой прихода снижает уверенность и не вычитается
  полностью как надежный приход.
- Unit: `не закупать` исключает строку до расчета заказа.
- Unit: новинка не масштабируется по первой продаже.
- Unit: неликвид не уходит в автозакупку.
- Unit: АКБ без MSDS/UN38.3/сертификата/ТН ВЭД/invoice/packing list блокируется.
- Integration: supplier resolver не создает дубль CRM-поставщика.
- Integration: Bitrix-карточка получает контур закупки, блокеры и evidence.
- Integration: файл обмена создает только черновик 1С.
- Manual acceptance: закупка сверяет 20-50 строк пилота с ручным решением.

# Review Notes / Risks

- Главный риск - автоматом купить лишнее из-за неправильной средней продажи.
- Второй риск - выбрать поставщика по старому или дубльному признаку.
- Третий риск - пропустить документы по АКБ/ВЭД.
- Четвертый риск - сделать красивую карточку Bitrix без реального контракта
  данных.
- Пятый риск - смешать пилот АКБ и общий контур закупок в один неуправляемый
  проект.
- Шестой риск - считать сезонность по общей сумме продаж без учета того, сколько
  SKU было активно в периоде.
- Седьмой риск - не учесть потребности магазинов из РММЗ/TopControl и получить
  красивый расчет, который не закрывает реальные точки.

На review обязательно смотреть:

- не появилась ли прямая запись в SQL 1С;
- не проводится ли документ 1С автоматически;
- есть ли простая расшифровка терминов;
- учитываются ли дни без товара отдельно от нулевого спроса;
- не считается ли центральный склад торговой точкой;
- не раздувает ли MOQ заказ без ручной проверки;
- есть ли `confidence_score` и понятный порог автозаказа;
- все ли блокеры доходят до пользователя;
- можно ли по строке понять, почему программа предложила именно это решение.
- видно ли, какие риски трехлетней закупки изменили количество или остановили
  автозаказ.

# Rollout

1. Включить read-only расчет для пилотной папки.
2. Сверить рекомендации с закупкой.
3. Включить evidence и supplier resolver в dry-run.
4. Включить Bitrix-карточки без 1С.
5. Включить файловый обмен в тестовом режиме.
6. Разрешить создание черновика 1С только после подтверждения.
7. Провести 30-дневный пилот.
8. По итогам пилота расширить на следующую папку.

Rollback:

- отключить создание файла обмена;
- оставить Bitrix-карточки и read-only расчет;
- вернуть все спорные строки в ручную закупку;
- не удалять историю evidence и решений.

# Черновик задачи Омару

Название: `[Закупки] Дисплеи: подтвердить правила профилей для автозаказа`

Ответственный: Омар Бочаров.

Цель: подтвердить правила по дисплеям после того, как программа разберет заказы
поставщику за последние 6 месяцев, продажи и карточки номенклатуры.

Что программа должна посчитать сама до ответа Омара:

- список строк, где помощник закупок дает высокий `К ЗАКАЗУ`;
- какие строки надо исключить до заказа: `дубль`, `снятый`, замена, ручной флаг
  `не закупать`, качество/бренд под вопросом;
- свойства дисплеев из карточек/наименований: качество, рамка, площадка под IC,
  проверка/верификация, совместимость;
- `предмет`, `tag`, характеристики и группы аналогов, если они заполнены в
  карточках достаточно надежно;
- расчетную единицу по каждой строке: SKU, аналог, `предмет + tag`, свойства,
  поставщик, потребность магазина или ручной профиль;
- метод спроса по каждой строке: средняя, сезонная средняя, BRT, потребность
  магазина или ручная проверка;
- какие дисплеи реально заказывались вместе по заказам поставщику;
- срок `заказ поставщику -> сдача в cargo`;
- срок `сдача в cargo -> поступление на склад`;
- дорогие/средние/дешевые дисплеи через 4 группы себестоимости;
- быстрые/медленные товары по фактическим датам;
- pipeline открытых заказов: в компании, около 10 дней, около 20 дней, сдано в
  cargo, у поставщика;
- флагманы в приоритете: штуки, сумма продаж, валовая прибыль, маржа,
  себестоимость;
- широкий или разовый спрос: по магазинам, клиентам/контрагентам и доле одного
  крупнейшего клиента;
- редкий, но реальный спрос, где BRT стоит сравнить с обычной средней;
- новые, юные, обычные, старые и неликвидные позиции;
- потребности магазинов из заказов покупателя вида `Потребности Савелова`.

Короткие вопросы Омару после расчета:

1. Подтверждаешь, что первый пилот делаем только по папке `дисплеи`?
2. Какие из найденных стопов неверные: `дубль`, `снятый`, замена, `не закупать`?
3. Какие расчетные единицы верные: SKU, аналог, `предмет + tag`, свойства,
   поставщик или потребность магазина?
4. Какие свойства/характеристики действительно должны дробить расчет и заказ?
5. Где BRT по редкому спросу выглядит лучше обычной средней, а где завышает
   заказ?
6. Какие дисплеи из рассчитанных групп нельзя пускать в автозаказ никогда?
7. Какие дорогие товары после пилота можно пускать в автозаказ с ручной
   страховкой, а какие оставлять только на ручной проверке?
8. Какие дешевые флагманы можно пробовать заказывать крупнее и реже?
9. Какие новинки по дисплеям всегда должны идти через ручную проверку?
10. У каких дисплеев есть MOQ/кратность/минимальная партия, которую программа не
   увидит сама?
11. Какие свойства дисплеев реально определяют поставщика: качество, рамка,
   площадка под IC, верификация, другое?
12. Какие поставщики по дисплеям разрешены для автозаказа, а какие только для
   ручной проверки?
13. Где в карточке заказа поставщику точно заполнена дата сдачи в cargo, если
   программа найдет несколько похожих дат?
14. Какие результаты расчета по дисплеям Омар считает неверными и почему?
15. Какие профили дисплеев после пилота можно пробовать в автозаказ?
16. Какие профили дисплеев оставляем только на ручной проверке?

Результат задачи: подтвержденная таблица `дисплеи -> расчетная единица -> метод
спроса -> профиль -> признаки -> сроки -> min/max -> стопы -> можно ли
автозаказ`.

# Открытые вопросы до пилота

Закрытые решения от 2026-06-25:

- `Эксклюзив` ставит и снимает Омар как старший отдела закупки; на сайте такие
  товары нужно показывать и выделять как эксклюзивные.
- `Ручной минимальный остаток` для `Эксклюзив` задают Омар и ответственный
  менеджер по папке; общего дефолта нет. Если остаток не задан, товар уходит в
  ручную проверку, а не в автозаказ. Поле оставляем общим, чтобы позже применять
  его не только к эксклюзиву.
- Пересмотр `Эксклюзив` - каждые 30 дней, подтверждают те же ответственные.
- Текущий ежедневный документ потребности по подразделению сохраняется, но для
  новинок нужен отдельный персональный ТЗ/документ, где все менеджеры видят
  список и добавляют свои сигналы.
- Ответственный за папку хранится в настройках папки и отвечает за ручную
  дорогую классификацию, подтверждение переходов и страховой запас.
- Дорогой товар определяется по верхним 25% цены/себестоимости внутри группы;
  профиль может ставиться автоматически из строки заказа поставщику или вручную
  ответственным за папку при первой работе с номенклатурой.
- Точкой продаж считается любой склад, откуда товар продается систематически,
  кроме центрального склада и складов брака/несистемных продаж; для этого нужна
  отдельная доработка/настройка в 1С.
- Для перехода в `Рабочий` не вводим дополнительный блокер "слабые продажи":
  5 поступлений за 180 дней от `Новинки` считаются достаточным сигналом, дальше
  остается подтверждение ответственного за папку.
- Вопрос бонусов не реализуется в этом проекте: сейчас собираем факты по
  персональным сигналам менеджеров и подозрительным завышениям, веса и выплаты
  будут в отдельном проекте по ЗП/KPI.

Эти вопросы не блокируют улучшение формулы, но без ответов часть строк будет
попадать в ручную проверку, а не в автозаказ.

1. Какие точные свойства номенклатуры в 1С уже заполнены достаточно хорошо,
   чтобы строить по ним профиль закупочного поведения?
2. Где в карточках надежно заполнены `предмет`, `tag`, характеристики и группы
   аналогов?
3. Какие признаки можно использовать как расчетную единицу закупки сразу, а
   какие требуют ручной вычитки?
4. Для каких профилей редкого спроса BRT разрешен, а для каких запрещен?
5. Какой целевой уровень наличия ставим для BRT по разным профилям?
6. Где в 1С надежнее брать свободный и фактический остаток для всех нужных
   складов и где вести признак "системная точка продаж / центральный / брак /
   несистемная продажа"?
7. Какие свойства дисплеев точно должны дробить заказ по поставщикам:
   качество, рамка, площадка под IC, верификация или другой признак?
8. Есть ли календарь акций/сезонности, который должен повышать спрос до заказа?
9. Где точный источник плана продаж и прогноза продаж: TopControl, РММЗ,
   отдельная таблица или управленческий файл?
10. Где в РММЗ/TopControl лежат потребности магазинов вида `Потребности
   Савелова`, и какие из них считать активными?
11. Какой период сезонности главный для разных папок: месяц, неделя или оба
   варианта?
12. С какого confidence score разрешаем автосоздание черновика после пилота:
   `80`, `85`, `90`?
13. Какие папки вообще нельзя пускать в автозаказ без человека даже после пилота?
14. Бюджет закупки должен ограничивать заказ по папке, поставщику или общему
   дневному лимиту?
15. Как именно показываем `Эксклюзив` на сайте: бейдж в карточке, фильтр,
   отдельная подборка, баннер/лендинг или несколько вариантов сразу?

# Changelog

- 2026-06-23 - added assortment management layer for future `1С КА` migration:
  `assortment_passport.v1`, `assortment_policy_recommendation.v1`,
  assortment matrix, internal BCG, ABC/XYZ, new-item control and price segments.
- 2026-06-23 - replaced status-only UT 10.3 exchange draft with
  `nomenclature_property_updates.v1` for whitelist nomenclature property
  updates.
- 2026-06-23 - added calculation units, demand method auto-selection, BRT for
  rare confirmed demand, and fields/tests for `subject`, `tag`, properties and
  analog groups.
- 2026-06-22 - added three-year seasonality/assortment analysis, sales
  plan/forecast, store needs from RMMZ/TopControl, and procurement risk flags.
- 2026-06-22 - re-studied Disk videos 37976/37992 and added rules for `К
  ЗАКАЗУ`, `дубль`, `не закупать`, display properties, demand spread, pipeline,
  expensive/cheap flagships, new/young/nonliquid lifecycle.
- 2026-06-22 - formula strengthened to 9/10 pre-pilot target: demand quality,
  coverage horizon, supplier constraints, confidence score, open questions.
- 2026-06-22 - added procurement behavior profiles, free/factual 1C stock,
  hybrid min/max stock rules, multi-metric flagship classification, Omar task.
- 2026-06-22 - draft created from procurement onepage v4 and user clarifications.
